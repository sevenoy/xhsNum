# 版本号更新规则

## 📋 版本号格式

**格式**：`V + 年月日（6位） + . + 总更新次数`

### 格式说明

- **V**：固定前缀
- **年月日（6位）**：年份后2位 + 月份2位 + 日期2位
- **总更新次数**：从项目开始到现在的总更新次数（不是当天更新次数）

### 示例

- `V251217.37` - 2025年12月17日，总第37次更新
- `V251218.38` - 2025年12月18日，总第38次更新（跨天，总次数继续递增）
- `V251218.39` - 2025年12月18日，总第39次更新（同一天第二次更新，总次数继续递增）

## 🔄 更新规则

### 核心原则

**总更新次数是累计的，不是按天重置的**

- 每次更新版本号时，总更新次数 = 历史所有版本总数 + 1
- 即使跨天，总更新次数也继续递增，不会重置
- 版本号格式：`V + 当天日期 + . + 总更新次数`

### 示例说明

假设当前已有36个版本：

- 今天是 2025年12月17日，第37次更新：`V251217.37`
- 今天是 2025年12月17日，第38次更新：`V251217.38`（同一天第二次更新）
- 明天 2025年12月18日，第39次更新：`V251218.39`（跨天，总次数继续递增）

## 📝 需要更新的文件

每次更新版本号时，需要同时更新以下文件：

### 1. `index.html`

**位置**：第355行左右

```javascript
// ✅ 应用版本号 - 每次更新代码时修改这个版本号
// 格式：V + 年月日（6位） + . + 总更新次数
// 例如：V251217.37 表示 2025年12月17日，总第37次更新
const APP_VERSION = 'V251217.37';
```

### 2. `sw.js`

**位置**：第7行左右

```javascript
// ✅ 版本号格式：V + 年月日（6位） + . + 总更新次数
// 例如：V251217.37 表示 2025年12月17日，总第37次更新
const VERSION = 'V251217.37'; // ✅ 每次更新代码时，修改这个版本号
```

### 3. `update-log.json`

**位置**：文件开头，添加新的版本条目

```json
{
  "V251217.37": {
    "version": "V251217.37",
    "date": "2025-12-17",
    "title": "更新标题",
    "content": [
      "更新内容1",
      "更新内容2"
    ]
  },
  // ... 旧版本
}
```

**重要**：`sw.js` 中的 `VERSION` 必须与 `index.html` 中的 `APP_VERSION` **完全相同**！

## 🚀 使用自动更新脚本

### 方法1：使用 update-version.js（推荐）

脚本会自动：
1. 统计 `update-log.json` 中所有版本的总数
2. 获取当天日期
3. 计算新的总更新次数（总数 + 1）
4. 更新所有相关文件

```bash
# 自动计算并更新版本号
node update-version.js
```

### 方法2：手动指定版本号

```bash
# 手动指定版本号
node update-version.js V251217.37
```

## 📊 版本号统计

脚本会自动从 `update-log.json` 中统计所有版本，计算总更新次数。

统计规则：
- 统计所有 `"V251216.10"` 格式的版本
- 统计所有 `"20251212.8"` 格式的版本（旧格式）
- 总更新次数 = 统计到的版本总数 + 1

## ⚠️ 注意事项

1. **版本号必须一致**：`index.html` 和 `sw.js` 中的版本号必须完全相同
2. **总更新次数是累计的**：不会因为跨天而重置
3. **每次更新都要更新版本号**：即使是很小的改动
4. **更新后提交代码**：更新版本号后记得提交并推送到 GitHub

## 📅 版本号示例

假设项目从 2025年12月13日开始：

- 第1次更新：`V251213.1`
- 第2次更新：`V251213.2`
- ...
- 第10次更新：`V251213.10`
- 第11次更新：`V251214.11`（跨天，总次数继续递增）
- 第12次更新：`V251214.12`（同一天第二次更新，总次数继续递增）

