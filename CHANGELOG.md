# 更新日志

## [最新更新] - 2025-01-XX

### 🎉 新增功能

#### 1. 最近修改页面
- **新增文件**: `recent-changes.html`
- **功能描述**: 在管理中心新增"最近修改"页面，显示每次保存快照时的修改记录
- **主要特性**:
  - 显示所有快照保存历史
  - 自动对比相邻快照的差异
  - 显示新增、删除、修改的行
  - 显示修改用户信息和时间
  - 支持展开/收起查看详细修改内容
  - 统计总修改次数、新增行数、删除行数、修改行数
  - 显示字段级别的修改详情（电话号码、所属人、微信实名、小红书名称等）

#### 2. 搜索框清除功能
- **文件**: `index.html`, `js/app.js`, `css/style.css`
- **功能描述**: 在搜索输入框右侧添加清除按钮
- **主要特性**:
  - 当有输入内容时自动显示清除图标（×）
  - 点击可一键清空搜索内容
  - 悬停效果优化
  - 响应式设计，适配各种屏幕尺寸

### 🔧 功能优化

#### 1. 默认排序设置
- **文件**: `js/app.js`, `index.html`
- **修改内容**: 
  - 将默认排序从"手动排序"改为"所属人排序"
  - 所有用户读取列表时默认按所属人排序

#### 2. 修改历史显示优化
- **文件**: `recent-changes.html`
- **修改内容**:
  - 移除了修改历史中红色文字的删除线
  - 优化了修改记录的视觉展示
  - 改进了差异对比算法

#### 3. 手机端用户体验优化
- **文件**: `css/style.css`, `index.html`
- **修改内容**:
  - 修复了手机端用户名无法显示的问题
  - 优化了 topbar 在手机端的布局
  - 添加了响应式样式，确保用户名在所有设备上都能正常显示
  - 修复了手机端点击搜索框时自动放大的问题（设置字体大小为16px）

#### 4. 手机端 Panel 布局优化
- **文件**: `css/style.css`
- **修改内容**:
  - 优化了手机端（768px以下）panel 的 padding 和间距
  - 第一行工具栏（搜索和筛选）改为垂直排列，每个元素占满宽度
  - 优化了搜索框、按钮和下拉框的字体大小和间距
  - 小手机（480px以下）进一步优化：
    - 第二行工具栏（按钮行）使用两列网格布局
    - 所有按钮均匀分布，每行2个
    - 搜索框和下拉框全宽显示
    - 统一间距为 6px 或 8px
    - 优化了按钮文字溢出处理
  - 改进了整体视觉层次和可读性

#### 5. 手机端工具栏布局重新设计
- **文件**: `css/style.css`
- **修改内容**:
  - 第一行工具栏优化：
    - 搜索框和"当前：模糊搜索"按钮在同一行显示
    - 三个下拉框（所属人、微信实名人、排序）在同一行显示
    - 使用 flexbox 和 order 属性实现精确布局
  - 第二行工具栏优化：
    - 8个功能按钮使用4列网格布局
    - 自动分成2行，每行4个按钮
    - 包括：新增一行、清空全部、保存云端、云端加载、分类设置、显示设置、管理页面、导入导出
  - 小手机（480px以下）同样应用上述布局
  - 优化了按钮宽度和间距，提升可用性

#### 6. 修复手机端列表输入框自动放大问题
- **文件**: `css/style.css`
- **修改内容**:
  - 修复了手机端点击列表中的输入框时屏幕自动放大的问题
  - 将所有列表输入框字体大小设置为 16px（iOS Safari 不会自动放大的最小字体）
  - 包括：`.m-input`、`.m-textarea`、`.m-detail-value[contenteditable="true"]`、`.m-detail-value select`
  - 调整了输入框的 padding 和 line-height，确保16px字体下的美观性
  - 设置了最小高度 44px，提升可点击性
  - 在768px和480px媒体查询中都应用了修复

### 🐛 问题修复

#### 1. Icon 显示问题修复
- **文件**: `index.html`, `site.webmanifest`, `icon/site.webmanifest`, 所有 HTML 文件
- **问题**: 手机端添加到桌面时 icon 无法显示，显示为灰色
- **修复内容**:
  - 修复了 `index.html` 中 manifest 链接的拼写错误（`manifaest` → `manifest`）
  - 完善了 `site.webmanifest` 配置，添加了完整的 icons 配置
  - 修复了 `icon/site.webmanifest` 中的图标路径
  - 为所有 HTML 文件添加了 icon 相关的 meta 标签
  - 统一了 theme-color 配置

#### 2. 用户名显示问题修复
- **文件**: `css/style.css`, `index.html`
- **问题**: 用户名在手机端无法显示
- **修复内容**:
  - 添加了手机端 topbar 布局样式
  - 强制显示用户名元素（使用 `!important` 确保显示）
  - 优化了 flex 布局，防止元素被压缩
  - 调整了字体大小和间距，适配手机端

#### 3. 搜索框自动放大问题修复
- **文件**: `css/style.css`
- **问题**: 手机端点击搜索框时屏幕会自动放大
- **修复内容**:
  - 在手机端媒体查询中设置搜索框字体大小为 16px
  - 防止 iOS Safari 自动放大页面

### 📝 代码改进

#### 1. 管理中心功能扩展
- **文件**: `admin.html`
- **修改内容**:
  - 添加了"最近修改"按钮
  - 更新了路由配置
  - 优化了导航按钮布局

#### 2. 样式优化
- **文件**: `css/style.css`
- **修改内容**:
  - 添加了搜索框清除按钮样式
  - 优化了手机端 topbar 布局
  - 改进了响应式设计

### 📦 文件变更清单

#### 新增文件
- `recent-changes.html` - 最近修改页面
- `CHANGELOG.md` - 更新日志（本文件）

#### 修改文件
- `admin.html` - 添加最近修改按钮
- `index.html` - 搜索框清除功能、默认排序、用户名显示修复
- `js/app.js` - 默认排序设置、搜索框清除功能
- `css/style.css` - 手机端优化、搜索框样式、用户名显示修复、Panel 布局优化、工具栏布局重新设计、列表输入框自动放大修复
- `site.webmanifest` - 完善 icon 配置
- `icon/site.webmanifest` - 修复图标路径
- 所有其他 HTML 文件 - 添加 icon 相关配置

### 🔄 历史更新

#### 2025-01-XX - Icon 显示修复
- 修复了手机端添加到桌面时 icon 无法显示的问题
- 添加了 `.gitignore` 文件，忽略 macOS 系统文件

---

## 技术细节

### 最近修改页面实现
- 使用 Supabase 查询历史快照（key 以 `snap_` 开头）
- 实现快照差异对比算法
- 支持行级别的增删改检测
- 支持字段级别的修改详情显示

### 搜索框清除功能实现
- 使用 CSS 绝对定位实现清除按钮
- JavaScript 监听输入事件，动态显示/隐藏清除按钮
- 点击清除按钮时清空输入并触发重新渲染

### 手机端优化
- 使用媒体查询 `@media (max-width: 768px)` 和 `@media (max-width: 480px)`
- 使用 flexbox 布局确保元素正确显示
- 设置输入框字体大小为 16px 防止自动放大

---

## 使用说明

### 最近修改页面
1. 进入管理中心
2. 点击"最近修改"按钮
3. 查看所有快照保存历史
4. 点击"查看详情"查看具体修改内容

### 搜索框清除
1. 在搜索框输入内容
2. 右侧会自动显示清除图标（×）
3. 点击清除图标即可清空搜索内容

### 默认排序
- 页面加载时自动按"所属人"排序
- 可在排序下拉框中手动更改排序方式

---

## 注意事项

1. **Icon 配置**: 确保 `icon/` 文件夹中包含所有必需的图标文件
2. **手机端测试**: 建议在真实设备上测试手机端功能
3. **浏览器缓存**: 修改 icon 相关配置后，可能需要清除浏览器缓存
4. **权限要求**: 最近修改页面需要登录后才能访问

---

## 后续计划

- [ ] 优化快照差异对比算法性能
- [ ] 添加更多筛选和搜索功能
- [ ] 优化手机端用户体验
- [ ] 添加数据导出功能

