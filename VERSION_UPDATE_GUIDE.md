# 版本号更新指南

## 📋 版本号格式

**新格式**：`V + 年月日（6位） + . + 版本更改号`

### 格式说明

- **V**：固定前缀
- **年月日（6位）**：年份后2位 + 月份2位 + 日期2位
- **版本更改号**：当天第几次更新（从1开始递增）

### 示例

- `V251213.8` - 2025年12月13日第8次更新
- `V251214.9` - 2025年12月14日第9次更新
- `V251214.1` - 2025年12月14日第1次更新（如果跨天，版本号从1开始）

## 🔄 更新规则

### 同一天多次更新
- 第一次更新：`V251213.1`
- 第二次更新：`V251213.2`
- 第三次更新：`V251213.3`
- ...以此类推

### 跨天更新
- 如果今天是 12月13日，最后一次版本是 `V251213.8`
- 明天 12月14日第一次更新：`V251214.1`（版本号从1开始）
- 明天 12月14日第二次更新：`V251214.2`

## 📝 需要更新的文件

每次更新版本号时，需要同时更新以下3个文件：

### 1. `index.html`

**位置**：第338行左右

```javascript
// ✅ 应用版本号 - 每次更新代码时修改这个版本号
// 格式：V + 年月日（6位） + . + 版本更改号
// 例如：V251213.8 表示 2025年12月13日第8次更新
const APP_VERSION = 'V251213.8';
```

### 2. `sw.js`

**位置**：第4行左右

```javascript
// ✅ 版本号格式：V + 年月日（6位） + . + 版本更改号
// 例如：V251213.8 表示 2025年12月13日第8次更新
const VERSION = 'V251213.8'; // ✅ 每次更新代码时，修改这个版本号
```

### 3. `update-log.json`

**位置**：文件开头，添加新的版本条目

```json
{
  "V251213.8": {
    "version": "V251213.8",
    "date": "2025-12-13",
    "title": "更新标题",
    "content": [
      "更新内容1",
      "更新内容2"
    ]
  },
  // ... 旧版本
}
```

**重要**：`sw.js` 中的 `VERSION` 必须与 `index.html` 中的 `APP_VERSION` **完全相同**！

## 🚀 更新步骤

### 步骤1：确定版本号

1. 获取当前日期（年月日）
   ```bash
   date +"%y%m%d"
   # 输出：251213（表示2025年12月13日）
   ```

2. 确定版本更改号
   - 如果是今天第一次更新：`.1`
   - 如果是今天第二次更新：`.2`
   - 如果是今天第三次更新：`.3`
   - ...以此类推

3. 组合版本号
   - 格式：`V + 日期 + . + 版本号`
   - 示例：`V251213.8`

### 步骤2：更新文件

#### 更新 `index.html`

```javascript
const APP_VERSION = 'V251213.8'; // 修改这里的版本号
```

#### 更新 `sw.js`

```javascript
const VERSION = 'V251213.8'; // 修改这里的版本号
```

#### 更新 `update-log.json`

在文件开头添加新版本条目：

```json
{
  "V251213.8": {
    "version": "V251213.8",
    "date": "2025-12-13",
    "title": "本次更新的标题",
    "content": [
      "🔧 修复内容1",
      "✨ 新增功能1",
      "✅ 优化内容1"
    ]
  },
  // ... 保留旧版本
}
```

### 步骤3：提交代码

```bash
git add index.html sw.js update-log.json
git commit -m "更新版本号到 V251213.8: [简要描述更新内容]"
git push origin main
```

## 📅 版本号示例

### 2025年12月13日
- 第一次更新：`V251213.1`
- 第二次更新：`V251213.2`
- 第三次更新：`V251213.3`
- ...
- 第八次更新：`V251213.8`

### 2025年12月14日
- 第一次更新：`V251214.1`（跨天，从1开始）
- 第二次更新：`V251214.2`
- ...
- 第九次更新：`V251214.9`

## ⚠️ 注意事项

1. **版本号必须一致**：`index.html` 和 `sw.js` 中的版本号必须完全相同
2. **日期格式**：使用6位数字（年月日），例如：251213（2025年12月13日）
3. **版本更改号**：同一天内递增，跨天从1开始
4. **更新日志**：每次更新都要在 `update-log.json` 中添加新条目
5. **提交前检查**：确保所有3个文件都已更新

## 🔍 快速检查清单

更新版本号后，检查以下内容：

- [ ] `index.html` 中的 `APP_VERSION` 已更新
- [ ] `sw.js` 中的 `VERSION` 已更新（与 `APP_VERSION` 相同）
- [ ] `update-log.json` 中已添加新版本条目
- [ ] 版本号格式正确（V + 6位日期 + . + 版本号）
- [ ] 日期正确（当前日期）
- [ ] 版本更改号正确（当天第几次更新）

## 📚 相关文件

- `index.html` - 主页面，包含 `APP_VERSION`
- `sw.js` - Service Worker，包含 `VERSION`
- `update-log.json` - 更新日志，包含所有版本记录
- `site.webmanifest` - PWA 配置，包含 `version` 字段（可选更新）

---

**最后更新**：2025-12-13  
**当前版本格式**：V + 年月日（6位） + . + 版本更改号
