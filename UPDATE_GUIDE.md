# 📱 iOS Web App 自动更新指南

## 🎯 问题说明

当你在 iOS 上"添加到主屏幕"后，Web App 会缓存资源。当你更新代码后，用户打开主屏幕图标时可能看不到最新版本，因为没有刷新按钮。

## ✅ 解决方案

我已经为你实现了**自动更新机制**，包括：

1. **Service Worker** - 智能缓存控制
2. **版本号管理** - 每次更新代码时修改版本号即可
3. **自动检测更新** - 每5分钟自动检查，页面可见时也会检查
4. **用户友好提示** - 发现新版本时自动弹出更新提示

## 📝 如何使用

### 每次更新代码后，只需做3步：

#### 1. 更新版本号（3个地方）

**① Service Worker 版本号** (`sw.js` 第4行)
```javascript
const VERSION = '20250115.1'; // 修改这个版本号，例如：20250115.2
```

**② HTML 中的版本号** (`index.html` 第318行)
```javascript
const APP_VERSION = '20250115.1'; // 修改这个版本号，与上面保持一致
```

**③ Manifest 版本号** (`site.webmanifest` 第11行)
```json
"version": "20250115.1",  // 修改这个版本号，与上面保持一致
"start_url": "./index.html?v=20250115",  // 同时更新这里的版本号
```

**④ CSS 版本号** (`index.html` 第22行)
```html
<link rel="stylesheet" href="./css/style.css?v=20250115" />
```

#### 2. 提交代码并部署

正常提交代码到服务器即可。

#### 3. 用户端自动更新

- **自动检测**：用户打开 App 时，系统会自动检测新版本
- **更新提示**：发现新版本时，会弹出友好的更新提示
- **一键更新**：用户点击"立即更新"按钮即可

## 🔧 版本号命名规则

建议使用格式：`YYYYMMDD.N`

- `YYYYMMDD`：日期（例如：20250115）
- `N`：当天第几次更新（例如：.1, .2, .3）

示例：
- `20250115.1` - 2025年1月15日第一次更新
- `20250115.2` - 2025年1月15日第二次更新
- `20250116.1` - 2025年1月16日第一次更新

## 🎨 工作原理

1. **Service Worker 注册**：页面加载时自动注册 Service Worker
2. **版本检测**：比较本地缓存版本和服务器版本
3. **更新提示**：发现新版本时，自动弹出更新提示
4. **缓存清理**：更新时自动清理旧版本缓存
5. **页面刷新**：更新完成后自动刷新页面

## 📱 iOS 特殊说明

### 添加到主屏幕的步骤：
1. 在 Safari 中打开网站
2. 点击分享按钮（底部中间）
3. 选择"添加到主屏幕"
4. 输入名称并添加

### 更新机制：
- ✅ 自动检测：每次打开 App 时自动检查
- ✅ 后台检测：每5分钟自动检查一次
- ✅ 可见时检测：页面从后台切回前台时检查
- ✅ 用户提示：发现新版本时友好提示

## 🐛 故障排除

### 如果更新不生效：

1. **清除浏览器缓存**
   - iOS Safari：设置 → Safari → 清除历史记录和网站数据

2. **重新添加到主屏幕**
   - 删除旧图标
   - 重新添加新图标

3. **检查版本号**
   - 确保所有3个地方的版本号都已更新
   - 确保版本号格式正确

4. **检查 Service Worker**
   - 打开 Safari 开发者工具
   - 查看 Console 是否有错误

## 💡 最佳实践

1. **每次更新代码都更新版本号** - 即使是很小的改动
2. **版本号保持一致** - 确保3个地方的版本号相同
3. **测试更新流程** - 更新后自己测试一下是否生效
4. **告知用户** - 重大更新时可以在更新提示中说明更新内容

## 🔍 调试技巧

### 查看当前版本：
打开浏览器控制台，查看：
```
✅ Service Worker 注册成功，版本: 20250115.1
```

### 手动触发更新检查：
在控制台执行：
```javascript
navigator.serviceWorker.getRegistration().then(reg => reg.update());
```

### 清除所有缓存：
在控制台执行：
```javascript
caches.keys().then(names => Promise.all(names.map(name => caches.delete(name))));
```

## 📚 技术细节

- **Service Worker**：用于控制缓存策略
- **Cache API**：存储和更新缓存资源
- **版本号机制**：通过版本号区分新旧版本
- **网络优先策略**：HTML/CSS/JS 使用网络优先，确保获取最新版本
- **自动清理**：新版本激活时自动删除旧版本缓存

---

**提示**：这个方案已经过优化，适用于 iOS Safari 添加到主屏幕的场景。
