# 更新日志

## [最新更新] - 2025-12-13

### 🔧 问题修复

#### 1. 修复前端不自动更新且不弹出提示框的问题
- **问题**: 前端不自动更新最新的云端版本，而且不弹出版本提示框
- **原因分析**:
  - `showUpdateNotification` 函数中的检查逻辑可能阻止显示
  - 检测到新版本时，清除记录的逻辑不够彻底
  - 状态栏检测到新版本时，清除记录的条件判断可能不够全面
- **修复内容**:
  - 强制清除记录：检测到新版本时，无论什么情况都强制清除已显示记录
  - 修复显示逻辑：优化 `showUpdateNotification` 函数的检查逻辑，确保检测到新版本时能正常显示
  - 添加调试日志：增加详细的日志输出，方便排查问题
  - 修复状态栏检测：状态栏检测到新版本时，强制清除记录，确保能显示更新提示
- **文件**: `index.html`, `js/app.js`
- **版本**: V251213.37

### 🔧 问题修复（历史）

#### 1. 修复V251213.34版本客户端无法自动更新的问题
- **问题**: V251213.34版本的所有客户端都没有自动更新到V251213.35版本
- **原因分析**:
  - 检测到新版本时，清除已显示记录的逻辑不够彻底
  - `showUpdateNotification` 函数中的检查逻辑可能阻止显示
  - 清除操作后立即调用 `showUpdateNotification`，可能读取到旧值
- **修复内容**:
  - 强制清除记录：检测到新版本时，强制清除已显示记录，无论什么情况都清除
  - 延迟清除操作：清除记录后延迟100ms，确保清除操作完成
  - 优化更新日志读取：如果当前版本更新日志不存在，尝试使用最新版本的更新日志
  - 修复显示逻辑：确保在检测到新版本时，即使之前显示过，也能再次显示
- **文件**: `index.html`
- **版本**: V251213.36

### ⚡ 性能优化

#### 1. 优化快照版本检查间隔时间
- **优化内容**: 将快照版本检查间隔从5分钟缩短到2分钟
- **效果**: 
  - 更快检测到云端快照更新
  - 用户能更快地看到云端快照的更新
  - 提升使用体验
- **文件**: `js/app.js`
- **版本**: V251213.35

### 🔧 问题修复

#### 1. 修复更新时不显示对话框的问题
- **问题**: 更新最新版本时，不弹出对话框，用户看不到更新提示
- **原因分析**:
  - 之前的自动更新实现完全跳过了对话框显示
  - 用户无法看到更新提示和更新内容
- **修复内容**:
  - 恢复更新对话框显示：检测到新版本时，先显示更新对话框，让用户看到更新提示
  - 自动更新流程：显示对话框后，延迟1秒自动执行更新流程，无需用户点击确定
  - 清除已显示记录：确保新版本提示能够正常显示，不会因为已显示记录而跳过
- **文件**: `index.html`
- **版本**: V251213.34

### 🚀 新功能

#### 1. 检测到新版本时立即自动更新
- **功能**: 所有手机端检测到最新版本时，立即启动自动更新程序，无需用户点击确定
- **实现方式**:
  - 检测到版本不匹配时，立即触发 Service Worker 更新检查
  - 如果发现 waiting 状态的 Service Worker，立即发送 `SKIP_WAITING` 消息激活
  - 如果发现 installing 状态的 Service Worker，等待安装完成后立即激活
  - 如果以上都不存在，尝试强制重新注册 Service Worker
  - 更新前自动清除所有缓存，确保获取最新代码
  - Service Worker 激活后，自动刷新页面以应用新版本
  - 如果自动更新失败，降级显示更新提示让用户手动更新
- **优势**:
  - 用户无需任何操作，检测到新版本后自动更新
  - 更新过程快速、流畅，用户体验更好
  - 多重更新策略确保在各种情况下都能成功更新
- **文件**: `index.html`, `js/app.js`
- **版本**: V251213.33

### 🔧 问题修复

#### 1. 修复苹果手机端快照更新需要点击5次的问题
- **问题**: 在苹果手机端，检测到有新的云端快照时，用户点击更新，点击确定，需要点击5次才能生效
- **原因分析**:
  - `checkAndUpdateSnapshot` 函数可能被多次调用（页面可见、窗口焦点等事件）
  - `cloudLoad` 函数可能被重复调用
  - 快照选择点击事件可能被多次触发
  - 确认对话框可能被多次弹出
  - 没有防重复调用机制
- **修复内容**:
  - 防止重复调用：添加 `isCheckingSnapshot` 和 `isCloudLoading` 标志，防止快照检查和加载函数被重复调用
  - 防止重复确认对话框：检查是否已有确认对话框在显示，避免重复弹出
  - 创建覆盖层：在显示 `confirm` 对话框前创建覆盖层，防止重复调用
  - 事件处理优化：在快照选择点击事件中添加防重复机制，使用 `capture` 模式防止事件冒泡
  - 标志清除机制：在所有 `return` 语句和 `finally` 块中正确清除标志，确保不会永久锁定
  - 延迟清除标志：延迟1-2秒清除标志，避免快速连续调用
- **文件**: `js/app.js`
- **版本**: V251213.32

### 🔧 问题修复（历史）

#### 1. 修复手机端检测到新版本但不自动更新的问题
- **问题**: 所有手机端都可以检测出新的版本，但是不启动自动更新，过了十分钟还是没有更新最新版本
- **原因分析**:
  - 检测到版本不匹配后，Service Worker 可能没有正确更新
  - `reg.update()` 可能没有触发 Service Worker 进入 waiting 状态
  - 更新提示可能被某些条件阻止显示
  - 已显示记录可能阻止了新版本提示的显示
- **修复内容**:
  - 强制触发 Service Worker 更新：检测到版本不匹配时，强制触发 Service Worker 更新检查
  - 强制重新注册：如果 Service Worker 没有进入 waiting 状态，尝试强制清除缓存并重新注册
  - 简化更新提示逻辑：简化已显示记录的检查逻辑，10分钟内不重复提示，超过10分钟允许重新显示
  - 直接刷新机制：如果没有 waiting 的 Service Worker 但版本不匹配，直接刷新页面
  - 优化状态栏版本检查：检测到新版本时立即清除已显示记录，确保新版本提示能显示
  - 延迟重试机制：如果更新检查函数不可用，延迟2秒后重试
- **文件**: `index.html`, `js/app.js`
- **版本**: V251213.31

### ✨ 功能增强

#### 1. 云端加载按钮1分钟自动关闭激活状态
- **功能**: 如果云端加载按钮被激活但没有选择快照，1分钟后自动关闭激活状态
- **实现内容**:
  - 云端加载自动关闭：启动1分钟定时器，如果用户没有选择快照，自动关闭激活状态
  - 智能定时器管理：启动定时器后，如果用户选择了快照或关闭面板，自动清除定时器
  - 自动关闭面板：1分钟超时后，自动关闭云端历史面板
  - 清除激活状态：超时后自动清除按钮的激活状态，恢复初始状态
  - 完整生命周期管理：在按钮点击、快照选择、面板关闭等所有场景下正确管理定时器
- **文件**: `js/app.js`
- **版本**: V251213.30

### 🔧 问题修复

#### 1. 修复首次使用快照加载提示框重复弹出问题
- **问题**: 首次使用时，快照加载提示框一直弹出，用户点击取消后仍然重复弹出
- **原因分析**:
  - 用户点击取消后，代码没有记录用户的选择
  - 每次检查时都会再次弹出提示框
  - 没有机制防止重复提示
- **修复内容**:
  - 添加拒绝标志管理：用户点击取消后，记录拒绝标志和拒绝时间
  - 24小时内不再弹出：如果用户最近（24小时内）拒绝过，不再弹出提示
  - 加载成功后清除标志：用户成功加载快照后，自动清除拒绝标志
  - 优化提示信息：在提示框中说明点击取消后24小时内不再提示
  - 智能检查逻辑：检查用户是否最近拒绝过，避免重复打扰用户
- **文件**: `js/app.js`
- **版本**: V251213.29

### ✨ 功能增强

#### 1. 添加快照版本检查和自动更新功能
- **功能**: 确保所有客户端读取的云端都是同一个最新快照，如果不是最新的，提示并自动更新
- **实现内容**:
  - 页面加载时自动检查本地快照是否是最新版本
  - 检测到云端有更新快照时，提示用户并自动加载最新快照
  - 每5分钟定期检查一次快照版本，确保所有客户端同步
  - 页面可见或获得焦点时立即检查快照版本
  - 首次使用时，如果云端有快照，提示用户是否加载
  - 检测到有未保存的本地修改时，提示用户确认后再更新
  - 显示本地和云端快照的时间、保存人等信息，便于用户判断
- **文件**: `js/app.js`
- **版本**: V251213.28

### ✨ 功能增强（历史）

#### 1. 状态栏检测到新版本时自动弹出更新提示
- **功能**: 当状态栏检测到云端有新版本时，自动触发更新检查并弹出更新提示
- **实现内容**:
  - 暴露全局函数：将 `checkForUpdate` 和 `showUpdateNotification` 暴露为全局函数，供状态栏调用
  - 保存 Service Worker 注册对象：保存到全局变量 `window.serviceWorkerRegistration`，确保状态栏可以访问
  - 自动清除旧记录：检测到新版本时，自动清除旧的显示记录，确保新版本提示能正常显示
  - 智能调用逻辑：优先调用 `checkForUpdate` 进行完整检查，如果不可用则直接调用 `showUpdateNotification`
  - 延迟触发：延迟 500ms 触发更新检查，确保状态栏已更新显示
- **文件**: `index.html`, `js/app.js`
- **版本**: V251213.27

### 🔧 问题修复

#### 1. 修复安卓端更新提示确定按钮无法点击问题
- **问题**: 在安卓设备上，更新版本提示无法通过点击确定按钮来关闭，必须刷新页面才能取消
- **原因分析**:
  - 按钮只绑定了 `click` 和 `touchend` 事件，缺少 `touchstart` 事件
  - overlay 的触摸事件拦截了按钮的触摸事件
  - 按钮的 z-index 和 pointer-events 可能被覆盖
  - 安卓浏览器的触摸事件处理机制与 iOS 不同
- **修复内容**:
  - 增强按钮事件绑定：添加 `touchstart` 和 `touchend` 事件处理
  - 使用 `{ passive: false, capture: true }` 确保事件能正确捕获
  - 修复 overlay 事件拦截：检查触摸目标，如果是按钮或其子元素，允许事件传播
  - 增强按钮样式：提高 z-index 到 `100002`，添加 `!important` 确保样式优先级
  - 安卓设备特殊处理：检测安卓设备并应用特殊处理，强制设置按钮样式
  - 添加 `mousedown` 事件作为备用
  - 优化事件处理函数：添加 `stopImmediatePropagation()` 防止事件重复触发
- **文件**: `index.html`
- **版本**: V251213.26

## [历史更新] - 2025-12-13

### 🔧 问题修复

#### 1. 修复添加到主屏幕后绕过登录问题
- **问题**: 在 iPhone X 等设备上添加到主屏幕后，点击桌面图标直接进入"号码管理"页面，而不是登录界面
- **原因分析**:
  - Service Worker 缓存了旧的 HTML，导致新的验证逻辑没有执行
  - `getSession()` 返回了过期的 session token，但代码没有验证其有效性
  - 在 standalone 模式下，浏览器缓存机制可能导致验证逻辑被绕过
- **修复内容**:
  - 在 standalone 模式下强制清除 Service Worker 缓存
  - 使用 `window.location.replace` 替代 `window.location.href`，避免浏览器缓存
  - 增强 session 验证：检测更多认证错误类型（JWT、expired、Invalid、Unauthorized）
  - 在 standalone 模式下，如果查询失败，进行额外的验证
  - 网络错误时也进行验证，确保 session 真的有效
- **文件**: `index.html`
- **版本**: V251213.25

#### 2. 修复 iPhone X 更新按钮不显示问题
- **问题**: iPhone X 上一直没有弹出更新按钮
- **原因分析**:
  - 更新提示的显示记录可能被缓存，导致新版本无法显示
  - iPhone X 的 Service Worker 更新检测可能有延迟
  - 时间戳记录缺失，无法判断是否需要重新显示
- **修复内容**:
  - 添加时间戳记录：记录更新提示的显示时间
  - iPhone X 特殊处理：如果超过 5 分钟未显示，强制重新显示（可能是缓存问题）
  - 版本检测优化：检测到新版本时，清除旧的显示记录和时间戳
  - 确保新版本提示能正常显示
  - 添加详细日志：记录 userAgent、standalone 状态等信息
- **文件**: `index.html`
- **版本**: V251213.25

#### 3. 添加状态栏版本定期检查功能
- **功能**: 状态信息栏每2分钟自动检查版本更新
- **内容**:
  - 每 2 分钟自动检查一次版本更新
  - 页面可见时立即检查
  - 窗口获得焦点时立即检查
  - 优化缓存绕过：使用 `cache: 'reload'` 模式，强制重新获取 `sw.js` 文件
- **文件**: `js/app.js`
- **版本**: V251213.25

## [历史更新] - 2025-12-12

### 🔧 问题修复

#### 1. 修复刷新循环问题
- **问题**: 点击确定更新按钮后，网站不停地刷新，无法正常使用
- **原因分析**:
  - 点击确定按钮时调用 `skipWaiting()` 触发 Service Worker 激活
  - `controllerchange` 事件监听器检测到 Service Worker 更新后再次触发刷新
  - 形成刷新循环：点击确定 → 刷新 → controllerchange → 再次刷新 → 循环
- **修复内容**:
  - 使用 `sessionStorage` 持久化刷新标志，防止刷新循环
  - 添加 `controllerchange` 事件防抖机制（`controllerChangeHandled` 标志），确保只刷新一次
  - 优化刷新逻辑：移除延迟刷新（`setTimeout`），直接刷新避免事件冲突
  - 页面加载时自动清除刷新标志（如果存在且是最近 5 秒内设置的）
  - 在 `controllerchange` 监听器中检查 `sessionStorage` 刷新标志，如果存在则跳过刷新
- **文件**: `index.html`
- **版本**: 20251212.2

#### 2. 修复更新提示框按钮问题
- **问题**: 
  - iPhone 16 Pro Max：弹出提示但无法点击确定按钮，按钮样式有问题（高度不够，边距不一致）
  - iPhone X：弹出提示但无法点击确定，一直在反复弹出提示，无法选中
  - 版本号日期显示错误（显示 2025-01-15，实际是 2025-12-12）
- **修复内容**:
  - 修复确定按钮样式：增加高度（`min-height: 48px`），统一上下左右边距（`padding: 16px 32px`）
  - 修复按钮无法点击问题：添加移动端触摸事件支持（`touchend`），添加 `touch-action: manipulation` 和 `pointer-events: auto`
  - 修复 z-index 问题：按钮 `z-index: 100001`，确保在最上层
  - 修复反复弹出问题：加强防重复显示逻辑，在显示前检查 `refreshing` 状态和 `updateOverlay` 是否存在
  - 优化事件绑定：等待 DOM 更新后再绑定事件，绑定 `click` 和 `touchend` 两种事件
  - 修复日期显示：使用 `new Date().toISOString().split('T')[0]` 自动获取当前日期
  - 更新版本号到 `20251212.1`，同步更新 `index.html`、`sw.js` 和 `update-log.json`
- **文件**: `index.html`, `sw.js`, `update-log.json`
- **版本**: 20251212.1

## [历史更新] - 2025-01-15

### ✨ 功能优化

#### 1. 增强版本更新机制
- **新增内容**:
  - 优化更新检测逻辑，确保每次版本变化都会显示更新提示
  - 修改版本号检测，当检测到版本不匹配时显示更新提示而不是直接刷新
  - 优化 localStorage 检查逻辑，允许同一版本在用户刷新后再次显示（如果用户未确认）
  - 创建版本号更新指南文档（`VERSION_UPDATE_GUIDE.md`），详细说明如何更新版本号
  - 更新 README.md，添加版本号更新指南链接和重要提醒
- **文件**: `index.html`, `VERSION_UPDATE_GUIDE.md`, `README.md`
- **版本**: 20250115.6

### 🔧 问题修复

#### 1. 修复不同设备更新不一致问题
- **问题**: 同样的网址，同样的桌面书签网站，iPhone X 自动更新了代码，但 iPhone 16 Pro Max 没有更新到最新版本
- **修复内容**:
  - 增强更新检测机制，添加强制版本号检测
  - 通过 fetch 获取 `sw.js` 文件内容并解析版本号，与当前 `APP_VERSION` 比较
  - 如果版本不匹配，自动注销并重新注册 Service Worker
  - Service Worker 注册时添加时间戳参数，确保每次都能获取最新版本
  - 添加多重检查时机：页面加载时、加载完成后延迟检查、首次加载后强制检查
  - 使用 `async/await` 确保更新检查完成，延迟 100ms 后再次检查状态
  - 更新日志文件获取时也添加时间戳，确保获取最新内容
- **文件**: `index.html`

## [历史更新] - 2025-01-15

### 🔧 问题修复

#### 1. 修复版本号显示问题
- **问题**: 版本号在刷新时显示了一下就马上隐藏了
- **修复内容**:
  - 修改 `applyView` 函数，使用 `innerHTML` 而不是 `textContent`，确保更新标题时保留版本号
  - 如果版本号不存在，从 `window.APP_VERSION` 读取并自动添加
  - 版本号现在能正确显示，即使 `applyView` 被调用也不会被移除
- **文件**: `js/app.js`, `index.html`

#### 2. 修复更新提示框自动消失问题
- **问题**: 更新提示框只出现了1秒钟就自动消失了，还没看清楚内容
- **修复内容**:
  - 移除 `controllerchange` 事件监听器中的自动刷新逻辑
  - 添加 `pendingUpdate` 标志，标记是否有待处理的更新
  - 只有在用户点击"确定"按钮后才刷新页面，确保用户能看到更新内容
  - 提高更新提示框 z-index 到 99999，确保在最上层显示
  - 添加 MutationObserver 保护机制，防止提示框被意外移除
  - 点击遮罩层不会关闭提示框，必须点击确定按钮
- **文件**: `index.html`

### ✨ 功能优化

#### 1. 在标题后添加版本号显示
- **新增内容**:
  - 在 `h1#appTitle`（"号码管理"）后添加版本号显示
  - 版本号格式：`v20250115.5`
  - 版本号使用小号灰色字体，桌面端12px，移动端11px
  - 版本号从 `APP_VERSION` 变量自动读取，无需手动更新HTML
  - 添加CSS样式确保版本号在桌面端和移动端都能正常显示
- **文件**: `index.html`, `css/style.css`

#### 2. 新增更新内容提示框功能
- **新增内容**:
  - 创建 `update-log.json` 文件存储更新日志
  - 更新提示框居中显示，包含版本号、日期和详细更新内容
  - 更新内容以列表形式展示，支持滚动查看
  - 只有一个"确定"按钮，点击后自动更新并刷新页面
  - 页面加载时和每5分钟自动检查更新
- **文件**: `index.html`, `update-log.json`

#### 2. 修复 Service Worker 自动更新机制
- **问题**: 手机上的网站没有自动更新到最新代码，关闭重新打开也没有更新
- **修复内容**:
  - Service Worker 注册时添加版本号查询参数（`./sw.js?v=版本号`），确保浏览器能检测到文件变化
  - 添加 `updateViaCache: 'none'` 禁用 Service Worker 脚本的 HTTP 缓存
  - 改进更新检测逻辑，监听多个状态（`installed`、`activating`、`activated`）
  - 增强更新检查函数，检查 `reg.waiting` 和 `reg.installing`
  - 页面加载时立即检查是否有等待中的更新
  - 每5分钟自动检查更新
  - 页面可见时检查更新
- **文件**: `index.html`, `sw.js`

### 📦 版本更新

- **Service Worker 版本**: `20250115.4` → `20250115.5`
- **Manifest 版本**: `20250115.4` → `20250115.5`
- **应用版本**: `20250115.4` → `20250115.5`

---

## [历史更新] - 2025-01-15

### 🔧 问题修复

#### 1. 修复移动端列表点击边框问题
- **问题**: 在移动端点击列表项时，会出现一个长方形的焦点边框
- **修复内容**:
  - 在 `.m-row` 和 `.m-row-header` 中添加 `outline: none !important`
  - 添加 `-webkit-tap-highlight-color: transparent` 移除移动端点击高亮
  - 为所有焦点状态（`:focus`、`:focus-visible`、`:focus-within`、`:active`）添加样式移除
  - 确保点击时不会出现任何焦点轮廓或边框变化
- **文件**: `css/style.css`

#### 2. 统一所有iPhone型号的topbar间距
- **问题**: iPhone X 在 standalone 模式下，topbar 与状态栏之间有空白间距，而 iPhone 16 Pro Max 是紧贴状态栏的
- **修复内容**:
  - 统一所有手机尺寸下的 topbar 间距为 `calc(env(safe-area-inset-top) + 2px) !important`
  - 移除 `.wrap` 的 `padding-top`，避免与 topbar 的 `padding-top` 叠加造成双重间距
  - 在 768px 和 480px 媒体查询中都统一了间距设置
  - 确保所有 iPhone 型号（包括 iPhone X、iPhone 16 Pro Max 等）在 standalone 模式下都紧贴状态栏，无空白间距
- **文件**: `css/style.css`

### ✨ 功能优化

#### 1. 管理中心添加刷新按钮
- **新增内容**:
  - 在管理中心标题栏添加"🔄 刷新"按钮
  - 位置：在"⚙️ 管理中心"标题和"返回主页"按钮之间
  - 功能：点击可手动刷新当前iframe内的页面内容
  - 样式：使用ghost样式，与返回主页按钮对齐
- **文件**: `admin.html`

#### 2. 优化快照浏览器自动刷新机制
- **问题**: 自动刷新在后台时可能仍会消耗电量和内存
- **优化内容**:
  - 新增 `isPageVisible()` 函数，多重检测页面是否真正可见
    - 检查 `document.hidden` 状态
    - 检查iframe中父窗口的可见性
    - 检查窗口焦点状态
  - 增强后台检测机制：
    - `visibilitychange` 事件监听
    - `focus`/`blur` 事件监听
    - iframe中父窗口焦点监听
    - `pagehide` 事件监听
  - 后台行为优化：
    - 页面在后台时立即停止自动刷新
    - 清除定时器，释放内存
    - 不发起网络请求，节省电量
  - 确保在后台（未激活）状态下完全不消耗电量和内存
- **文件**: `snapshot-browser.html`

### 🔧 问题修复

#### 1. 修复FOUC问题（Flash of Unstyled Content）
- **问题**: olena用户点击管理中心时，在页面加载的瞬间能看到"审核管理"、"用户管理"、"权限管理"三个按钮
- **修复内容**:
  - 在CSS中添加 `.admin-only` 类，默认隐藏管理员按钮（使用 `!important`）
  - 在HTML中给三个管理员按钮添加 `admin-only` 类
  - JavaScript中，如果是管理员才移除 `admin-only` 类显示按钮
  - 确保普通用户从页面加载开始就看不到这些按钮
- **文件**: `admin.html`

#### 2. 修复管理中心权限检查问题
- **问题**: olena用户登录后，刷新管理中心页面时会看到"审核管理"、"用户管理"、"权限管理"三个按钮，这些按钮应该只有sevenoy管理员才能看到
- **修复内容**:
  - 修复权限检查逻辑：优先从数据库查询用户权限，而不是依赖localStorage
  - 只有用户名为 `sevenoy` 或 `role` 为 `super_admin` 的用户才是管理员
  - 查询失败时明确设置为非管理员，确保安全性
  - 使用多重保护隐藏管理员按钮：设置display、visibility、hidden属性，并移除DOM元素
  - 同步更新localStorage，但以数据库查询为准
- **文件**: `admin.html`

#### 3. 修复手机端管理中心按钮布局
- **问题**: 手机端管理中心按钮布局不符合要求
- **修复内容**:
  - 在手机端（max-width: 480px）使用CSS Grid布局，实现两列排列
  - 确保普通用户看到的按钮顺序：
    - 第一排：快照浏览器、显示设置
    - 第二排：分类设置、最近修改
  - 由于管理员按钮会被移除，普通用户的按钮会自动按DOM顺序排列
- **文件**: `admin.html`

### 📦 版本更新

- **Service Worker 版本**: `20250115.4` (保持不变)
- **Manifest 版本**: `20250115.4` (保持不变)
- **应用版本**: `20250115.4` (保持不变)

---

## [历史更新] - 2025-12-11

### 🔧 问题修复

#### 1. 修复分类设置数据丢失问题
- **问题**: 分类设置的名称和颜色在加载云端快照后丢失
- **修复内容**:
  - 恢复分类设置跟随云端快照更新的逻辑
  - 如果云端快照有分类数据，使用云端的；否则保留本地的
  - 加载云端数据后，如果分类设置面板是打开的，自动重新渲染分类列表
  - 添加详细的调试日志，方便排查问题
- **文件**: `js/app.js`

#### 2. 修复 standalone 模式下 topbar 间距问题
- **问题**: 在 iPhone 上添加到主屏幕后，topbar 上方多出一段间距
- **修复内容**:
  - 在 standalone 模式下，调整 topbar 的 padding-top 为 `calc(env(safe-area-inset-top) + 8px)`
  - 确保 wrap 的 padding-top 为 0，避免双重间距
  - 在 768px 和 480px 媒体查询中都添加了 standalone 模式的特殊处理
- **文件**: `css/style.css`

#### 3. 修复分类设置面板打开时的错误
- **问题**: 点击"分类设置"按钮时出现 `Cannot read properties of null` 错误
- **修复内容**:
  - 添加对 `#panelView` 元素的存在性检查（显示设置已移至独立页面）
  - 避免访问不存在的元素导致的错误
- **文件**: `js/app.js`

#### 4. 增强分类设置的调试功能
- **新增内容**:
  - 在分类数据读取、解析、保存过程中添加详细日志
  - 在分类列表渲染时添加详细的调试信息
  - 显示每个分类的 id、name、color 信息
  - 显示生成的 HTML 内容和渲染状态
- **文件**: `js/app.js`

### 📦 版本更新

- **Service Worker 版本**: `20250115.3` → `20250115.4`
- **Manifest 版本**: `20250115.3` → `20250115.4`
- **应用版本**: `20250115.3` → `20250115.4`

---

## [历史更新] - 2025-01-15

### 🎉 新增功能

#### 1. 显示设置页面
- **新增文件**: `view-settings.html`
- **功能描述**: 新增独立的显示设置页面，允许用户自定义界面显示样式
- **主要特性**:
  - 基础设置：标题文字、字体选择
  - 颜色设置：标题颜色、按钮颜色、隔行颜色、粗体选项
  - 尺寸设置：行高、列宽调节（滑块控制）
  - 一键紧凑：快速应用紧凑布局
  - 恢复默认：一键恢复默认设置
  - 实时保存：所有设置实时保存到 localStorage
  - 响应式设计：完美适配手机端和桌面端

#### 2. 强制清除缓存工具
- **新增文件**: `force-clear-cache.html`
- **功能描述**: 提供一键清除缓存和 Service Worker 的工具页面
- **主要特性**:
  - 清除所有缓存
  - 取消注册 Service Worker
  - 重新加载页面
  - 一键执行所有操作
  - 实时显示操作结果

#### 3. 强制刷新缓存指南
- **新增文件**: `FORCE_REFRESH.md`
- **功能描述**: 详细的缓存刷新指南文档
- **主要内容**:
  - 浏览器强制刷新方法（Chrome/Edge/Safari/移动端）
  - Service Worker 缓存清除方法
  - 控制台强制更新代码
  - 禁用 Service Worker 临时方法
  - 验证更新步骤

#### 4. 最近修改页面
- **新增文件**: `recent-changes.html`
- **功能描述**: 在管理中心新增"最近修改"页面，显示每次保存快照时的修改记录
- **主要特性**:
  - 显示所有快照保存历史
  - 自动对比相邻快照的差异
  - 显示新增、删除、修改的行
  - 显示修改用户信息和时间
  - 支持展开/收起查看详细修改内容
  - 统计总修改次数、新增行数、删除行数、修改行数
  - 显示字段级别的修改详情（电话号码、所属人、微信实名、小红书名称等）

#### 5. 搜索框清除功能
- **文件**: `index.html`, `js/app.js`, `css/style.css`
- **功能描述**: 在搜索输入框右侧添加清除按钮
- **主要特性**:
  - 当有输入内容时自动显示清除图标（×）
  - 点击可一键清空搜索内容
  - 悬停效果优化
  - 响应式设计，适配各种屏幕尺寸

### 🔧 功能优化

#### 1. 默认排序设置
- **文件**: `js/app.js`, `index.html`
- **修改内容**: 
  - 将默认排序从"手动排序"改为"所属人排序"
  - 所有用户读取列表时默认按所属人排序

#### 2. 修改历史显示优化
- **文件**: `recent-changes.html`
- **修改内容**:
  - 移除了修改历史中红色文字的删除线
  - 优化了修改记录的视觉展示
  - 改进了差异对比算法

#### 3. 手机端用户体验优化
- **文件**: `css/style.css`, `index.html`
- **修改内容**:
  - 修复了手机端用户名无法显示的问题
  - 优化了 topbar 在手机端的布局
  - 添加了响应式样式，确保用户名在所有设备上都能正常显示
  - 修复了手机端点击搜索框时自动放大的问题（设置字体大小为16px）

#### 4. 手机端 Panel 布局优化
- **文件**: `css/style.css`
- **修改内容**:
  - 优化了手机端（768px以下）panel 的 padding 和间距
  - 第一行工具栏（搜索和筛选）改为垂直排列，每个元素占满宽度
  - 优化了搜索框、按钮和下拉框的字体大小和间距
  - 小手机（480px以下）进一步优化：
    - 第二行工具栏（按钮行）使用两列网格布局
    - 所有按钮均匀分布，每行2个
    - 搜索框和下拉框全宽显示
    - 统一间距为 6px 或 8px
    - 优化了按钮文字溢出处理
  - 改进了整体视觉层次和可读性

#### 5. 手机端工具栏布局重新设计
- **文件**: `css/style.css`
- **修改内容**:
  - 第一行工具栏优化：
    - 搜索框和"当前：模糊搜索"按钮在同一行显示
    - 三个下拉框（所属人、微信实名人、排序）在同一行显示
    - 使用 flexbox 和 order 属性实现精确布局
  - 第二行工具栏优化：
    - 8个功能按钮使用4列网格布局
    - 自动分成2行，每行4个按钮
    - 包括：新增一行、清空全部、保存云端、云端加载、分类设置、显示设置、管理页面、导入导出
  - 小手机（480px以下）同样应用上述布局
  - 优化了按钮宽度和间距，提升可用性

#### 6. 修复手机端列表输入框自动放大问题
- **文件**: `css/style.css`
- **修改内容**:
  - 修复了手机端点击列表中的输入框时屏幕自动放大的问题
  - 将所有列表输入框字体大小设置为 16px（iOS Safari 不会自动放大的最小字体）
  - 包括：`.m-input`、`.m-textarea`、`.m-detail-value[contenteditable="true"]`、`.m-detail-value select`
  - 调整了输入框的 padding 和 line-height，确保16px字体下的美观性
  - 设置了最小高度 44px，提升可点击性
  - 在768px和480px媒体查询中都应用了修复

#### 7. 管理中心和快照浏览器手机端优化
- **文件**: `admin.html`, `snapshot-browser.html`, `recent-changes.html`, `css/style.css`
- **修改内容**:
  - 管理中心标题字体优化：桌面端 20px，手机端 18px，小手机 16px
  - 快照浏览器手机端布局优化：
    - 刷新和自动刷新按钮在同一排显示
    - 删除自动刷新下方的返回主页按钮
    - 自动刷新默认勾选并自动启动
    - 统计栏（我的快照、授权快照、总计）在手机端同一排显示
    - 操作按钮（加载、预览、授权、删除）在小手机上排成一行，支持横向滚动
  - 最近修改页面统计框优化：
    - 4个统计框（总修改次数、新增行数、删除行数、修改行数）在手机端一排显示
    - 优化了字体大小和间距，适配小屏幕
  - 所有返回主页按钮改为蓝色（`#1990FF`），与导航按钮 active 状态颜色一致

### 🐛 问题修复

#### 1. Icon 显示问题修复
- **文件**: `index.html`, `site.webmanifest`, `icon/site.webmanifest`, 所有 HTML 文件
- **问题**: 手机端添加到桌面时 icon 无法显示，显示为灰色
- **修复内容**:
  - 修复了 `index.html` 中 manifest 链接的拼写错误（`manifaest` → `manifest`）
  - 完善了 `site.webmanifest` 配置，添加了完整的 icons 配置
  - 修复了 `icon/site.webmanifest` 中的图标路径
  - 为所有 HTML 文件添加了 icon 相关的 meta 标签
  - 统一了 theme-color 配置

#### 2. 用户名显示问题修复
- **文件**: `css/style.css`, `index.html`
- **问题**: 用户名在手机端无法显示
- **修复内容**:
  - 添加了手机端 topbar 布局样式
  - 强制显示用户名元素（使用 `!important` 确保显示）
  - 优化了 flex 布局，防止元素被压缩
  - 调整了字体大小和间距，适配手机端

#### 3. 搜索框自动放大问题修复
- **文件**: `css/style.css`
- **问题**: 手机端点击搜索框时屏幕会自动放大
- **修复内容**:
  - 在手机端媒体查询中设置搜索框字体大小为 16px
  - 防止 iOS Safari 自动放大页面

### 📝 代码改进

#### 1. 管理中心功能扩展
- **文件**: `admin.html`
- **修改内容**:
  - 添加了"最近修改"按钮
  - 更新了路由配置
  - 优化了导航按钮布局

#### 2. 样式优化
- **文件**: `css/style.css`
- **修改内容**:
  - 添加了搜索框清除按钮样式
  - 优化了手机端 topbar 布局
  - 改进了响应式设计

### 📦 文件变更清单

#### 新增文件
- `view-settings.html` - 显示设置页面
- `force-clear-cache.html` - 强制清除缓存工具
- `FORCE_REFRESH.md` - 强制刷新缓存指南
- `recent-changes.html` - 最近修改页面
- `CHANGELOG.md` - 更新日志（本文件）

#### 修改文件
- `admin.html` - 添加最近修改按钮、管理中心字体优化、手机端布局优化
- `index.html` - 搜索框清除功能、默认排序、用户名显示修复、版本号更新
- `js/app.js` - 默认排序设置、搜索框清除功能
- `css/style.css` - 手机端优化、搜索框样式、用户名显示修复、Panel 布局优化、工具栏布局重新设计、列表输入框自动放大修复、返回主页按钮颜色统一
- `snapshot-browser.html` - 手机端布局优化、自动刷新默认开启、删除返回主页按钮
- `recent-changes.html` - 统计框手机端布局优化、返回主页按钮颜色
- `approval-management.html` - 返回主页按钮颜色
- `user-management.html` - 返回主页按钮颜色
- `permission-management.html` - 返回主页按钮颜色
- `site.webmanifest` - 完善 icon 配置、版本号更新至 20250115.3
- `sw.js` - Service Worker 版本号更新至 20250115.3
- `icon/site.webmanifest` - 修复图标路径
- 所有其他 HTML 文件 - 添加 icon 相关配置

### 🔄 历史更新

#### 2025-01-XX - Icon 显示修复
- 修复了手机端添加到桌面时 icon 无法显示的问题
- 添加了 `.gitignore` 文件，忽略 macOS 系统文件

---

## 技术细节

### 最近修改页面实现
- 使用 Supabase 查询历史快照（key 以 `snap_` 开头）
- 实现快照差异对比算法
- 支持行级别的增删改检测
- 支持字段级别的修改详情显示

### 搜索框清除功能实现
- 使用 CSS 绝对定位实现清除按钮
- JavaScript 监听输入事件，动态显示/隐藏清除按钮
- 点击清除按钮时清空输入并触发重新渲染

### 手机端优化
- 使用媒体查询 `@media (max-width: 768px)` 和 `@media (max-width: 480px)`
- 使用 flexbox 布局确保元素正确显示
- 设置输入框字体大小为 16px 防止自动放大

---

## 使用说明

### 最近修改页面
1. 进入管理中心
2. 点击"最近修改"按钮
3. 查看所有快照保存历史
4. 点击"查看详情"查看具体修改内容

### 搜索框清除
1. 在搜索框输入内容
2. 右侧会自动显示清除图标（×）
3. 点击清除图标即可清空搜索内容

### 默认排序
- 页面加载时自动按"所属人"排序
- 可在排序下拉框中手动更改排序方式

---

## 注意事项

1. **Icon 配置**: 确保 `icon/` 文件夹中包含所有必需的图标文件
2. **手机端测试**: 建议在真实设备上测试手机端功能
3. **浏览器缓存**: 修改 icon 相关配置后，可能需要清除浏览器缓存
4. **权限要求**: 最近修改页面需要登录后才能访问

---

## 后续计划

- [ ] 优化快照差异对比算法性能
- [ ] 添加更多筛选和搜索功能
- [ ] 优化手机端用户体验
- [ ] 添加数据导出功能

