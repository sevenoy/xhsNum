# 小红书账号管理系统 (XHSPHONE) - 技术白皮书

> **版本**: V251217.67  
> **最后更新**: 2025年12月17日  
> **作者**: sevenoy

---

## 📋 目录

1. [项目概述](#项目概述)
2. [功能清单](#功能清单)
3. [技术架构](#技术架构)
4. [核心功能实现](#核心功能实现)
5. [数据存储方案](#数据存储方案)
6. [用户认证与权限](#用户认证与权限)
7. [云端同步机制](#云端同步机制)
8. [版本更新系统](#版本更新系统)
9. [PWA 实现](#pwa-实现)
10. [性能优化](#性能优化)
11. [安全机制](#安全机制)
12. [部署与维护](#部署与维护)

---

## 项目概述

### 项目简介

小红书账号管理系统（XHSPHONE）是一个基于 Web 的 PWA（Progressive Web App）应用，用于管理小红书账号相关信息，包括电话号码、所属人、微信实名、小红书名称等数据。系统支持多用户协作、云端同步、快照管理等功能。

### 核心特性

- ✅ **离线优先**：数据首先存储在本地 IndexedDB，支持离线使用
- ✅ **云端同步**：基于 Supabase 实现多设备实时同步
- ✅ **多用户协作**：支持多用户登录和权限管理
- ✅ **快照管理**：支持数据快照保存和恢复
- ✅ **PWA 支持**：可添加到主屏幕，支持离线使用
- ✅ **响应式设计**：完美适配手机端和桌面端
- ✅ **自动更新**：基于 Service Worker 的自动更新机制

### 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 | - | 页面结构 |
| CSS3 | - | 样式和响应式布局 |
| JavaScript (ES6+) | - | 业务逻辑 |
| Dexie.js | 4.0.8 | IndexedDB 封装库 |
| Supabase JS | 2.x | 后端服务客户端 |
| Service Worker | - | 离线缓存和自动更新 |

---

## 功能清单

### 1. 数据管理功能

#### 1.1 号码管理
- **新增号码**：支持添加新的账号记录
- **编辑号码**：支持在线编辑所有字段
- **删除号码**：支持单个或批量删除
- **排序功能**：支持手动拖拽排序
- **批量操作**：支持批量删除、清空等操作

#### 1.2 数据字段
- **电话号码**：主键字段，唯一标识
- **所属人**：账号归属人
- **微信实名人**：微信实名认证人
- **对应微信名**：微信账号名称
- **小红书名称**：小红书账号名称
- **备注**：额外备注信息
- **分类**：自定义分类标签（带颜色）

### 2. 搜索与筛选功能

#### 2.1 搜索功能
- **模糊搜索**：支持任意关键词搜索（默认）
- **精确搜索**：支持精确匹配模式
- **多字段搜索**：同时搜索电话号码、所属人、微信名、小红书名称、备注等字段
- **实时搜索**：输入即时显示结果

#### 2.2 筛选功能
- **所属人筛选**：按所属人筛选数据
- **微信实名人筛选**：按微信实名人筛选数据
- **分类筛选**：按分类标签筛选数据

#### 2.3 排序功能
- **手动排序**：按手动设置的顺序排序（默认）
- **所属人排序**：按所属人字母顺序排序
- **微信实名人排序**：按微信实名人字母顺序排序
- **电话号码排序**：按电话号码数字顺序排序
- **小红书名称排序**：按小红书名称字母顺序排序
- **分类排序**：按分类标签排序

### 3. 云端同步功能

#### 3.1 云端保存
- **手动保存**：点击"保存云端"按钮保存数据
- **冲突检测**：自动检测云端数据冲突
- **自动加载**：检测到冲突时自动加载最新数据
- **保存状态提示**：显示保存状态和结果

#### 3.2 云端加载
- **手动加载**：点击"云端加载"按钮加载数据
- **自动同步**：基于 Supabase Realtime 的自动同步
- **版本检测**：自动检测云端版本更新
- **快照提示**：检测到新快照时弹出提示

#### 3.3 云端快照
- **快照保存**：每次保存时创建快照
- **快照浏览**：浏览所有历史快照
- **快照恢复**：恢复到指定快照版本
- **快照信息**：显示快照创建时间、创建人等

### 4. 用户管理功能

#### 4.1 用户认证
- **用户登录**：基于 Supabase Auth 的登录系统
- **用户注册**：支持新用户注册
- **记住密码**：支持记住用户名功能
- **自动登录**：支持自动登录（基于 session）

#### 4.2 权限管理
- **角色系统**：支持 basic、admin、super_admin 角色
- **权限检查**：基于角色的权限检查
- **资源权限**：支持快照级别的权限控制
- **权限申请**：支持权限申请和审核流程

#### 4.3 用户信息
- **用户资料**：显示用户名、邮箱等信息
- **在线状态**：显示用户在线状态
- **操作记录**：记录用户操作历史

### 5. 显示设置功能

#### 5.1 界面自定义
- **标题文本**：自定义应用标题
- **标题颜色**：自定义标题颜色
- **按钮颜色**：自定义按钮颜色
- **字体设置**：自定义字体和字体粗细
- **行高设置**：自定义表格行高
- **列宽设置**：自定义表格列宽

#### 5.2 表格样式
- **斑马纹**：开启/关闭斑马纹显示
- **斑马纹颜色**：自定义斑马纹颜色
- **字体大小**：自定义字体大小

### 6. 分类管理功能

#### 6.1 分类设置
- **新增分类**：添加新的分类标签
- **编辑分类**：修改分类名称和颜色
- **删除分类**：删除不需要的分类
- **分类颜色**：为每个分类设置颜色

#### 6.2 分类应用
- **分类标记**：为数据行标记分类
- **分类筛选**：按分类筛选数据
- **分类显示**：在表格中显示分类颜色

### 7. 数据导入导出功能

#### 7.1 数据导出
- **CSV 导出**：导出为 CSV 格式
- **数据备份**：导出所有数据作为备份

#### 7.2 数据导入
- **CSV 导入**：从 CSV 文件导入数据
- **数据合并**：导入时合并现有数据
- **数据覆盖**：导入时覆盖现有数据

### 8. 管理中心功能

#### 8.1 快照浏览器
- **快照列表**：浏览所有历史快照
- **快照搜索**：搜索指定快照
- **快照详情**：查看快照详细信息
- **快照恢复**：恢复到指定快照

#### 8.2 用户管理（管理员）
- **用户列表**：查看所有用户
- **用户编辑**：编辑用户信息
- **用户删除**：删除用户账号
- **角色管理**：管理用户角色

#### 8.3 权限管理（管理员）
- **权限列表**：查看所有权限记录
- **权限授予**：授予用户权限
- **权限撤销**：撤销用户权限
- **权限审核**：审核权限申请

#### 8.4 审核管理（管理员）
- **申请列表**：查看所有权限申请
- **申请审核**：审核权限申请
- **申请拒绝**：拒绝权限申请

#### 8.5 最近修改
- **修改历史**：查看所有数据修改历史
- **修改详情**：查看修改详细信息
- **修改人信息**：显示修改人信息

### 9. 版本更新功能

#### 9.1 自动更新检测
- **版本检测**：自动检测新版本
- **更新提示**：发现新版本时弹出提示
- **更新日志**：显示更新内容
- **强制更新**：支持强制更新机制

#### 9.2 版本管理
- **版本号规则**：`V + 年月日（6位） + . + 总更新次数`
- **版本记录**：记录所有版本更新历史
- **版本对比**：对比不同版本差异

---

## 技术架构

### 架构模式

本项目采用 **前后端分离 + PWA** 架构：

```
┌─────────────────────────────────────────┐
│         前端应用层 (Static HTML)         │
│  ┌──────────┐  ┌──────────┐  ┌──────┐  │
│  │ IndexedDB│  │localStorage│ │PWA   │  │
│  │ (Dexie)  │  │  (配置)   │ │Cache │  │
│  └──────────┘  └──────────┘  └──────┘  │
└─────────────────────────────────────────┘
                    ↕ HTTP/HTTPS
┌─────────────────────────────────────────┐
│      后端服务层 (Supabase)              │
│  ┌──────────┐  ┌──────────┐  ┌──────┐  │
│  │ Auth     │  │ Database │  │Storage│ │
│  │ (认证)   │  │ (Postgres)│ │ (可选)│ │
│  └──────────┘  └──────────┘  └──────┘  │
└─────────────────────────────────────────┘
```

### 数据流

1. **本地优先**：数据首先存储在 IndexedDB（Dexie）
2. **云端同步**：通过 Supabase 实现多设备数据同步
3. **离线支持**：Service Worker 提供离线缓存能力
4. **权限控制**：基于 Supabase Auth + 自定义权限表

### 文件结构

```
xhsNum/
├── index.html              # 主页面
├── login.html              # 登录页面
├── admin.html              # 管理中心
├── view-settings.html      # 显示设置
├── snapshot-browser.html   # 快照浏览器
├── user-management.html     # 用户管理
├── permission-management.html  # 权限管理
├── approval-management.html   # 审核管理
├── category-settings.html     # 分类设置
├── recent-changes.html        # 最近修改
├── css/
│   └── style.css          # 样式文件
├── js/
│   ├── app.js             # 主应用逻辑
│   ├── auth.js            # 认证模块
│   ├── config.js          # 配置模块
│   ├── data.js            # 数据操作模块
│   ├── sync.js            # 同步模块
│   ├── syncBridge.js      # 同步桥接
│   ├── supabaseClient.js  # Supabase 客户端
│   └── utils.js           # 工具函数
├── sw.js                   # Service Worker
├── site.webmanifest        # PWA 清单文件
├── update-log.json        # 更新日志
└── update-version.js      # 版本更新脚本
```

---

## 核心功能实现

### 1. 数据管理实现

#### 1.1 IndexedDB 数据库设计

**数据库名称**: `xhs_phone_sheet_v7`

**表结构**:

```javascript
db.version(2).stores({
  rows: "id,order,phone,owner,wx_real,wx_name,xhs_name,note1,row_color,updated_at",
  meta: "key"
});
```

**数据模型**:

```javascript
{
  id: string,           // 唯一ID
  order: number,        // 排序序号
  phone: string,        // 电话号码（主键）
  owner: string,        // 所属人
  wx_real: string,      // 微信实名人
  wx_name: string,      // 对应微信名
  xhs_name: string,     // 小红书名称
  note1: string,        // 备注
  row_color: string,    // 分类ID
  updated_at: number    // 更新时间戳
}
```

#### 1.2 数据操作函数

**读取数据**:
```javascript
async function getAllRows() {
  await ensureDbReady();
  const all = await db.rows.toArray();
  all.sort((a, b) => (a.order ?? 0) - (b.order ?? 0));
  return all;
}
```

**添加数据**:
```javascript
async function addRow(rowData) {
  markDirty();
  await ensureDbReady();
  await db.rows.add(rowData);
}
```

**更新数据**:
```javascript
async function updateRow(id, patch, options = {}) {
  markDirty();
  await ensureDbReady();
  const row = await db.rows.get(id);
  if (!row) return;
  await db.rows.put({ ...row, ...patch, updated_at: Date.now() });
  if (options.renderTable) await options.renderTable();
}
```

**删除数据**:
```javascript
async function deleteRowById(id) {
  markDirty();
  await ensureDbReady();
  await db.rows.delete(id);
}
```

**批量操作**:
```javascript
async function overwriteAllRows(rows) {
  await ensureDbReady();
  await db.transaction('rw', db.rows, async (tx) => {
    await tx.rows.clear();
    if (rows && rows.length > 0) {
      const batchSize = 100;
      for (let i = 0; i < rows.length; i += batchSize) {
        const batch = rows.slice(i, i + batchSize);
        await tx.rows.bulkAdd(batch);
      }
    }
  });
}
```

### 2. 搜索与筛选实现

#### 2.1 搜索算法

**模糊搜索**（默认）:
```javascript
function tokenize(text) {
  if (!text) return [];
  return text.toLowerCase().split(/\s+/).filter(t => t.length > 0);
}

function fuzzyMatch(row, queryTokens) {
  const searchableText = [
    row.phone, row.owner, row.wx_real, row.wx_name,
    row.xhs_name, row.note1
  ].join(' ').toLowerCase();
  
  return queryTokens.every(token => searchableText.includes(token));
}
```

**精确搜索**:
```javascript
function exactMatch(row, query) {
  const searchableText = [
    row.phone, row.owner, row.wx_real, row.wx_name,
    row.xhs_name, row.note1
  ].join(' ').toLowerCase();
  
  return searchableText.includes(query.toLowerCase());
}
```

#### 2.2 筛选实现

```javascript
function filterRows(rows, filters) {
  return rows.filter(row => {
    // 所属人筛选
    if (filters.owner !== 'all' && row.owner !== filters.owner) {
      return false;
    }
    
    // 微信实名人筛选
    if (filters.wxReal !== 'all' && row.wx_real !== filters.wxReal) {
      return false;
    }
    
    // 分类筛选
    if (filters.category !== 'all' && row.row_color !== filters.category) {
      return false;
    }
    
    return true;
  });
}
```

#### 2.3 排序实现

```javascript
function sortRows(rows, sortBy) {
  const sorted = [...rows];
  
  switch (sortBy) {
    case 'owner':
      sorted.sort((a, b) => (a.owner || '').localeCompare(b.owner || ''));
      break;
    case 'wx_real':
      sorted.sort((a, b) => (a.wx_real || '').localeCompare(b.wx_real || ''));
      break;
    case 'phone':
      sorted.sort((a, b) => (a.phone || '').localeCompare(b.phone || ''));
      break;
    case 'xhs_name':
      sorted.sort((a, b) => (a.xhs_name || '').localeCompare(b.xhs_name || ''));
      break;
    case 'row_color':
      sorted.sort((a, b) => (a.row_color || '').localeCompare(b.row_color || ''));
      break;
    case 'order':
    default:
      sorted.sort((a, b) => (a.order ?? 0) - (b.order ?? 0));
      break;
  }
  
  return sorted;
}
```

### 3. UI 渲染实现

#### 3.1 桌面端表格渲染

```javascript
async function renderTable() {
  const rows = await getAllRows();
  const filtered = filterAndSearch(rows);
  const sorted = sortRows(filtered, state.sortBy);
  
  const tbody = document.getElementById('gridBody');
  tbody.innerHTML = '';
  
  sorted.forEach(row => {
    const tr = makeRowTr(row);
    tbody.appendChild(tr);
  });
}

function makeRowTr(row) {
  const tr = document.createElement('tr');
  tr.innerHTML = `
    <td class="col-phone">${tdEditable(row, 'phone')}</td>
    <td class="col-owner">${tdEditable(row, 'owner')}</td>
    <td class="col-real">${tdEditable(row, 'wx_real')}</td>
    <td class="col-wx">${tdEditable(row, 'wx_name')}</td>
    <td class="col-xhs">${tdEditable(row, 'xhs_name')}</td>
    <td class="col-note">${tdEditable(row, 'note1')}</td>
    <td class="col-cat">${tdSelectCat(row)}</td>
    <td class="col-act">${tdActions(row)}</td>
  `;
  return tr;
}
```

#### 3.2 移动端列表渲染

```javascript
async function renderMobileList() {
  const rows = await getAllRows();
  const filtered = filterAndSearch(rows);
  const sorted = sortRows(filtered, state.sortBy);
  
  const container = document.getElementById('mobileList');
  container.innerHTML = '';
  
  sorted.forEach(row => {
    const card = makeMobileCard(row);
    container.appendChild(card);
  });
}

function makeMobileCard(row) {
  const card = document.createElement('div');
  card.className = 'mobile-card';
  card.innerHTML = `
    <div class="card-header">
      <span class="phone">${escapeHtml(row.phone)}</span>
      <span class="category" style="background: ${catColorOf(cats, row.row_color)}">
        ${catNameOf(cats, row.row_color)}
      </span>
    </div>
    <div class="card-body">
      <div class="field">
        <span class="label">所属人:</span>
        <span class="value">${escapeHtml(row.owner)}</span>
      </div>
      <!-- 更多字段... -->
    </div>
    <div class="card-actions">
      <button onclick="editRow('${row.id}')">编辑</button>
      <button onclick="deleteRow('${row.id}')">删除</button>
    </div>
  `;
  return card;
}
```

---

## 数据存储方案

### 1. 本地存储

#### 1.1 IndexedDB（Dexie）

**用途**: 存储主要数据（号码记录）

**优势**:
- 大容量存储（通常几 GB）
- 异步操作，不阻塞 UI
- 支持索引和查询
- 支持事务

**实现**:
```javascript
const db = new Dexie(DB_NAME);
db.version(2).stores({
  rows: "id,order,phone,owner,wx_real,wx_name,xhs_name,note1,row_color,updated_at",
  meta: "key"
});
```

#### 1.2 localStorage

**用途**: 存储配置和用户信息

**存储内容**:
- 视图配置（`xhs_view_v7`）
- 分类配置（`xhs_cats_v7`）
- 用户信息（`xhs_user_name`, `xhs_user_id` 等）
- Supabase 配置（`xhs_supabase_url`, `xhs_supabase_anon_key`）
- 同步时间戳（`last_sync_time`）
- 快照名称（`last_snapshot_name`）

**实现**:
```javascript
// 保存配置
localStorage.setItem(VIEW_KEY, JSON.stringify(viewConfig));

// 读取配置
const viewConfig = JSON.parse(localStorage.getItem(VIEW_KEY) || '{}');
```

### 2. 云端存储

#### 2.1 Supabase Database

**表结构**: `xhsphone_snapshot`

**字段**:
- `key` (text, PRIMARY KEY): 快照键（如 "default"）
- `owner_id` (uuid): 所有者用户ID
- `payload` (jsonb): 数据负载（包含 rows, cats, view）
- `updated_at` (timestamp): 更新时间
- `updated_by_name` (text): 更新人名称

**索引**:
- `key` 字段唯一索引
- `updated_at` 字段索引（用于排序）

**RLS 策略**:
- 用户只能查看自己拥有或有权限的快照
- 用户只能修改自己拥有的快照

#### 2.2 数据格式

**快照负载结构**:
```json
{
  "ver": 1,
  "rows": [
    {
      "id": "xxx",
      "phone": "13800138000",
      "owner": "张三",
      "wx_real": "李四",
      "wx_name": "微信名",
      "xhs_name": "小红书名",
      "note1": "备注",
      "row_color": "category_id",
      "order": 0,
      "updated_at": 1234567890
    }
  ],
  "cats": [
    {
      "id": "category_id",
      "name": "分类名",
      "color": "#007aff"
    }
  ],
  "view": {
    "pad": 4,
    "colScale": 0.7,
    "zebraOn": true,
    "zebraColor": "#e2f0ff",
    "fontFamily": "PingFang SC",
    "fontWeight": "normal",
    "titleText": "号码管理",
    "titleColor": "#208BEE",
    "btnColor": "#639BD5"
  },
  "snapshot_label": "用户名 202512172057"
}
```

---

## 用户认证与权限

### 1. 认证流程

#### 1.1 登录流程

```javascript
// 1. 用户输入邮箱和密码
const { data, error } = await supabase.auth.signInWithPassword({
  email: email,
  password: password
});

// 2. 获取用户信息
const { data: profile } = await supabase
  .from('user_profiles')
  .select('*')
  .eq('user_id', data.user.id)
  .single();

// 3. 存储用户信息到 localStorage
localStorage.setItem('xhs_user_name', profile.username);
localStorage.setItem('xhs_user_id', data.user.id);
localStorage.setItem('xhs_user_email', profile.email);
localStorage.setItem('xhs_user_role', profile.role || 'basic');

// 4. 检查管理员权限
const isAdmin = profile.role === 'super_admin' || profile.username === 'sevenoy';
localStorage.setItem('xhs_is_admin', isAdmin ? 'true' : 'false');
```

#### 1.2 注册流程

```javascript
// 1. 用户输入邮箱和密码注册
const { data, error } = await supabase.auth.signUp({
  email: email,
  password: password
});

// 2. 创建用户资料
const { error: profileError } = await supabase
  .from('user_profiles')
  .insert({
    user_id: data.user.id,
    email: email,
    username: username,
    role: 'basic'
  });
```

#### 1.3 登录检查

```javascript
// 在 index.html 中检查登录状态
const { data: { session } } = await supabase.auth.getSession();

if (!session) {
  window.location.href = 'login.html';
  return;
}

// 从数据库获取用户信息
const { data: profile } = await supabase
  .from('user_profiles')
  .select('*')
  .eq('user_id', session.user.id)
  .single();
```

### 2. 权限系统

#### 2.1 角色定义

- **basic**: 普通用户，只能查看和编辑自己的数据
- **admin**: 管理员，可以管理用户和权限
- **super_admin**: 超级管理员，拥有所有权限

#### 2.2 权限检查

```javascript
// 检查是否是管理员
export function isAdmin() {
  return window.isAdmin || localStorage.getItem('xhs_is_admin') === 'true';
}

// 检查资源权限
export async function checkPermission(resourceId, resourceType, permissionType = 'view') {
  const { data: { session } } = await supabase.auth.getSession();
  const authUid = session?.user?.id;
  
  // 检查是否是资源所有者
  if (resourceType === 'snapshot') {
    const { data: snapshot } = await supabase
      .from(SUPABASE_TABLE)
      .select('owner_id')
      .eq('key', resourceId)
      .maybeSingle();
    
    if (snapshot?.owner_id === authUid) {
      return true;
    }
  }
  
  // 检查权限表
  const { data: permission } = await supabase
    .from('permissions')
    .select('*')
    .eq('resource_id', resourceId)
    .eq('resource_type', resourceType)
    .eq('user_id', authUid)
    .eq('status', 'active')
    .maybeSingle();
  
  if (!permission) {
    return false;
  }
  
  // 检查权限类型
  if (permissionType === 'edit') {
    return permission.permission_type === 'edit';
  }
  
  return permission.permission_type === 'view' || permission.permission_type === 'edit';
}
```

#### 2.3 权限表结构

**表名**: `permissions`

**字段**:
- `id` (uuid, PRIMARY KEY): 权限ID
- `resource_id` (text): 资源ID（如快照 key）
- `resource_type` (text): 资源类型（如 'snapshot'）
- `user_id` (uuid): 用户ID
- `permission_type` (text): 权限类型（'view' 或 'edit'）
- `status` (text): 状态（'active', 'expired', 'revoked'）
- `expired_at` (timestamp): 过期时间
- `created_at` (timestamp): 创建时间

---

## 云端同步机制

### 1. 同步策略

#### 1.1 LWW（Last Write Wins）策略

系统采用 **LWW（Last Write Wins）** 策略处理冲突：
- 基于服务器时间戳判断最新数据
- 如果云端数据更新，自动加载云端数据
- 如果本地数据更新，保存到云端

#### 1.2 冲突检测

```javascript
// 保存前检测冲突
async function cloudSave() {
  // 1. 获取云端最新数据
  const { data: cloudData } = await supabase
    .from(SUPABASE_TABLE)
    .select('payload, updated_at, updated_by_name')
    .eq('key', SUPABASE_DEFAULT_KEY)
    .order('updated_at', { ascending: false })
    .limit(1);
  
  const cloudRow = cloudData?.[0];
  
  // 2. 比较数据内容
  const currentRows = await getAllRows();
  const latestRows = cloudRow?.payload?.rows || [];
  
  // 标准化并比较
  const rowsEqual = JSON.stringify(normalizeRows(currentRows)) === 
                    JSON.stringify(normalizeRows(latestRows));
  
  // 3. 检查时间戳
  const cloudTime = new Date(cloudRow.updated_at).getTime();
  const dataContentEqual = rowsEqual && catsEqual && viewEqual;
  
  if (dataContentEqual) {
    // 数据内容相同，无需保存
    if (cloudTime > lastSyncTimestamp) {
      // 但时间戳更新了，更新本地时间戳
      lastSyncTimestamp = cloudTime;
      localStorage.setItem('last_sync_time', String(cloudTime));
    }
    alert('没有数据改动');
    return;
  }
  
  // 4. 检测冲突
  if (cloudTime > lastSyncTimestamp) {
    // 云端数据更新，自动加载
    await cloudLoad(SUPABASE_DEFAULT_KEY, false);
    alert('已自动加载云端最新数据');
    return;
  }
  
  // 5. 保存数据
  await supabase.from(SUPABASE_TABLE).upsert({
    key: SUPABASE_DEFAULT_KEY,
    owner_id: userId,
    payload: payload,
    updated_at: new Date().toISOString(),
    updated_by_name: userName
  });
}
```

### 2. 自动同步

#### 2.1 Realtime 监听

```javascript
// 初始化自动同步
export async function initAutoSync() {
  const channel = supabase
    .channel('smart-auto-sync')
    .on(
      'postgres_changes',
      { 
        event: '*', 
        schema: 'public', 
        table: SUPABASE_TABLE, 
        filter: `key=eq.${SUPABASE_DEFAULT_KEY}` 
      },
      async (evt) => {
        const newRow = evt.new;
        if (!newRow) return;
        
        const serverTime = new Date(newRow.updated_at).getTime();
        const currentLastSync = parseInt(localStorage.getItem('last_sync_time') || '0');
        
        // 检查是否有新快照
        const currentSnapshotName = getLastSnapshotName();
        const newSnapshotName = newRow.payload?.snapshot_label || 
          generateSnapshotName(newRow.updated_by_name || '未知用户', newRow.updated_at);
        
        const snapshotNameChanged = currentSnapshotName !== newSnapshotName;
        
        if (snapshotNameChanged || serverTime > currentLastSync) {
          // 检测到新快照，弹出提示
          const ok = confirm(`检测到最新快照已更新\n\n"${newRow.updated_by_name}" 刚刚保存了新快照。\n\n点击【确定】自动加载最新快照`);
          
          if (ok) {
            await cloudLoad(SUPABASE_DEFAULT_KEY, false);
          }
        }
      }
    )
    .subscribe();
}
```

#### 2.2 快照名称检测

系统通过快照名称变化检测新快照：
- 快照名称格式：`用户名 202512172057`
- 每次保存时生成新的快照名称
- 通过比较快照名称判断是否有新快照

```javascript
function generateSnapshotName(userName, updatedAt) {
  const d = new Date(updatedAt);
  const Y = d.getFullYear();
  const M = String(d.getMonth() + 1).padStart(2, '0');
  const D = String(d.getDate()).padStart(2, '0');
  const h = String(d.getHours()).padStart(2, '0');
  const m = String(d.getMinutes()).padStart(2, '0');
  return `${userName} ${Y}${M}${D}${h}${m}`;
}
```

### 3. 数据加载

#### 3.1 手动加载

```javascript
export async function cloudLoad(keyOrSilent = SUPABASE_DEFAULT_KEY, silent = false) {
  // 1. 检查本地是否有未保存修改
  if (!isSilent && isLocalDirty()) {
    const ok = confirm('本地有未保存修改。加载云端数据将覆盖本地修改。是否继续？');
    if (!ok) return;
  }
  
  // 2. 查询云端最新数据
  const { data, error } = await supabase
    .from(SUPABASE_TABLE)
    .select('payload, updated_at, updated_by_name')
    .eq('key', key)
    .order('updated_at', { ascending: false })
    .limit(1)
    .maybeSingle();
  
  if (!data) {
    if (!isSilent) alert('云端无数据');
    return;
  }
  
  // 3. 检查时间戳（静默模式）
  const serverTime = new Date(data.updated_at).getTime();
  if (isSilent && serverTime <= lastSyncTimestamp) {
    return; // 云端数据不比本地新，跳过
  }
  
  // 4. 写入本地数据库
  await overwriteAllRows(data.payload.rows || []);
  
  // 5. 更新配置
  if (data.payload.cats) {
    saveCats(data.payload.cats, true);
  }
  if (data.payload.view) {
    saveView(data.payload.view, true);
  }
  
  // 6. 更新时间戳
  lastSyncTimestamp = serverTime;
  localStorage.setItem('last_sync_time', String(serverTime));
  
  // 7. 刷新界面
  if (window.renderTable) await window.renderTable();
  if (window.refreshFilters) await window.refreshFilters();
}
```

---

## 版本更新系统

### 1. 版本号规则

**格式**: `V + 年月日（6位） + . + 总更新次数`

**示例**:
- `V251217.37` - 2025年12月17日，总第37次更新
- `V251218.38` - 2025年12月18日，总第38次更新（跨天，总次数继续递增）

**规则**:
- 总更新次数是累计的，不会因为跨天而重置
- 每次更新版本号时，总更新次数 = 历史所有版本总数 + 1

### 2. 版本更新流程

#### 2.1 更新脚本

```javascript
// update-version.js
function getNewVersion() {
  const now = new Date();
  const year = String(now.getFullYear()).slice(-2);
  const month = String(now.getMonth() + 1).padStart(2, '0');
  const day = String(now.getDate()).padStart(2, '0');
  const datePart = `${year}${month}${day}`;
  
  // 读取 update-log.json 统计总版本数
  const updateLogPath = path.join(__dirname, 'update-log.json');
  let totalUpdates = 0;
  if (fs.existsSync(updateLogPath)) {
    const updateLogContent = fs.readFileSync(updateLogPath, 'utf8');
    const updateLog = JSON.parse(updateLogContent);
    totalUpdates = Object.keys(updateLog).length;
  }
  
  return `V${datePart}.${totalUpdates + 1}`;
}
```

#### 2.2 需要更新的文件

1. **index.html**: 更新 `APP_VERSION` 常量
2. **sw.js**: 更新 `VERSION` 常量
3. **site.webmanifest**: 更新 `version` 字段
4. **update-log.json**: 添加新版本条目

### 3. Service Worker 更新检测

#### 3.1 注册 Service Worker

```javascript
// index.html
if ('serviceWorker' in navigator) {
  const swUrl = './sw.js?v=' + APP_VERSION + '&t=' + Date.now();
  navigator.serviceWorker.register(swUrl, {
    scope: './',
    updateViaCache: 'none'
  }).then((reg) => {
    // 立即检查更新
    checkForUpdate(reg);
    
    // 监听更新
    reg.addEventListener('updatefound', () => {
      const newWorker = reg.installing;
      newWorker.addEventListener('statechange', () => {
        if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
          showUpdateNotification();
        }
      });
    });
  });
}
```

#### 3.2 更新检测

```javascript
async function checkForUpdate(reg) {
  await reg.update();
  
  // 检查是否有等待中的新版本
  if (reg.waiting) {
    showUpdateNotification();
    return;
  }
  
  // 检查 Service Worker 文件版本
  const swResponse = await fetch('./sw.js?v=' + APP_VERSION + '&t=' + Date.now(), {
    cache: 'no-store'
  });
  const swText = await swResponse.text();
  const versionMatch = swText.match(/const VERSION = ['"]([^'"]+)['"]/);
  
  if (versionMatch) {
    const serverVersion = versionMatch[1];
    if (serverVersion !== APP_VERSION) {
      showUpdateNotification();
    }
  }
}
```

#### 3.3 更新提示

```javascript
async function showUpdateNotification() {
  // 检查是否已显示过
  const lastShownVersion = localStorage.getItem('update_notification_shown');
  if (lastShownVersion === APP_VERSION) {
    return;
  }
  
  // 读取更新日志
  const response = await fetch('./update-log.json?v=' + APP_VERSION + '&t=' + Date.now());
  const updateLog = await response.json();
  const updateInfo = updateLog[APP_VERSION] || {
    title: '发现新版本',
    content: ['本次更新包含了一些改进和修复']
  };
  
  // 创建提示框
  const overlay = document.createElement('div');
  overlay.id = 'updateOverlay';
  overlay.innerHTML = `
    <div class="notification">
      <div class="title">${updateInfo.title}</div>
      <div class="content">
        ${updateInfo.content.map(item => `<div>${item}</div>`).join('')}
      </div>
      <button id="btnUpdateConfirm">确定</button>
    </div>
  `;
  document.body.appendChild(overlay);
  
  // 绑定确认按钮
  document.getElementById('btnUpdateConfirm').addEventListener('click', async () => {
    // 清除缓存
    const cacheNames = await caches.keys();
    await Promise.all(cacheNames.map(name => caches.delete(name)));
    
    // 通知 Service Worker 跳过等待
    if (registration && registration.waiting) {
      registration.waiting.postMessage({ type: 'SKIP_WAITING' });
    }
    
    // 刷新页面
    window.location.reload();
  });
}
```

---

## PWA 实现

### 1. Service Worker

#### 1.1 缓存策略

**关键资源**（网络优先）:
- HTML 文件
- CSS 文件
- JavaScript 文件
- Manifest 文件

**其他资源**（缓存优先）:
- 图片文件
- 字体文件
- 其他静态资源

```javascript
// sw.js
self.addEventListener('fetch', (event) => {
  const { request } = event;
  
  // 关键资源：网络优先
  if (request.destination === 'document' || 
      request.url.includes('.css') || 
      request.url.includes('.js')) {
    event.respondWith(
      fetch(request).then((response) => {
        if (response && response.status === 200) {
          const responseToCache = response.clone();
          caches.open(CACHE_NAME).then((cache) => {
            cache.put(request, responseToCache);
          });
        }
        return response;
      }).catch(() => {
        return caches.match(request);
      })
    );
  } else {
    // 其他资源：缓存优先
    event.respondWith(
      caches.match(request).then((cachedResponse) => {
        return cachedResponse || fetch(request).then((response) => {
          if (response && response.status === 200) {
            const responseToCache = response.clone();
            caches.open(CACHE_NAME).then((cache) => {
              cache.put(request, responseToCache);
            });
          }
          return response;
        });
      })
    );
  }
});
```

#### 1.2 缓存清理

```javascript
// 激活时清理旧缓存
self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((cacheNames) => {
      return Promise.all(
        cacheNames.map((cacheName) => {
          if (cacheName !== CACHE_NAME) {
            return caches.delete(cacheName);
          }
        })
      );
    })
  );
  return self.clients.claim();
});
```

### 2. Web App Manifest

```json
{
  "name": "号码管理",
  "short_name": "号码管理",
  "description": "小红书账号管理系统",
  "start_url": "./index.html",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#1990FF",
  "orientation": "portrait-primary",
  "icons": [
    {
      "src": "./icon/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "./icon/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 3. iOS Web App 支持

```html
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
<meta name="apple-mobile-web-app-title" content="号码管理" />
<link rel="apple-touch-icon" sizes="192x192" href="./icon/icon-192.png" />
<link rel="apple-touch-icon" sizes="512x512" href="./icon/icon-512.png" />
```

---

## 性能优化

### 1. 数据库优化

#### 1.1 批量操作

```javascript
// 批量添加数据（分批处理）
async function overwriteAllRows(rows) {
  await db.transaction('rw', db.rows, async (tx) => {
    await tx.rows.clear();
    if (rows && rows.length > 0) {
      const batchSize = 100;
      for (let i = 0; i < rows.length; i += batchSize) {
        const batch = rows.slice(i, i + batchSize);
        await tx.rows.bulkAdd(batch);
      }
    }
  });
}
```

#### 1.2 索引优化

```javascript
// 为常用查询字段创建索引
db.version(2).stores({
  rows: "id,order,phone,owner,wx_real,wx_name,xhs_name,note1,row_color,updated_at",
  meta: "key"
});
```

### 2. 渲染优化

#### 2.1 虚拟滚动（可选）

对于大量数据，可以考虑使用虚拟滚动：
- 只渲染可见区域的数据
- 减少 DOM 节点数量
- 提高滚动性能

#### 2.2 防抖和节流

```javascript
// 搜索防抖
let searchTimeout;
function handleSearch(query) {
  clearTimeout(searchTimeout);
  searchTimeout = setTimeout(() => {
    performSearch(query);
  }, 300);
}

// 滚动节流
let scrollTimeout;
window.addEventListener('scroll', () => {
  if (scrollTimeout) return;
  scrollTimeout = setTimeout(() => {
    handleScroll();
    scrollTimeout = null;
  }, 100);
});
```

### 3. 网络优化

#### 3.1 请求合并

```javascript
// 合并多个请求
async function loadInitialData() {
  const [rows, cats, view] = await Promise.all([
    getAllRows(),
    readCats(),
    readView()
  ]);
  return { rows, cats, view };
}
```

#### 3.2 缓存策略

- 使用 Service Worker 缓存静态资源
- 使用 localStorage 缓存配置数据
- 使用 IndexedDB 缓存主要数据

---

## 安全机制

### 1. 认证安全

#### 1.1 JWT Token

- 使用 Supabase Auth 的 JWT Token
- Token 自动过期和刷新
- 存储在内存中，不持久化敏感信息

#### 1.2 Session 管理

```javascript
// 检查 Session
const { data: { session } } = await supabase.auth.getSession();

if (!session) {
  window.location.href = 'login.html';
  return;
}

// 监听 Session 变化
supabase.auth.onAuthStateChange((event, session) => {
  if (event === 'SIGNED_OUT' || !session) {
    window.location.href = 'login.html';
  }
});
```

### 2. 数据安全

#### 2.1 RLS（Row Level Security）

Supabase 数据库使用 RLS 策略保护数据：

```sql
-- 用户只能查看自己拥有或有权限的快照
CREATE POLICY "Users can view their own snapshots"
ON xhsphone_snapshot
FOR SELECT
USING (
  owner_id = auth.uid() OR
  EXISTS (
    SELECT 1 FROM permissions
    WHERE permissions.resource_id = xhsphone_snapshot.key
    AND permissions.user_id = auth.uid()
    AND permissions.status = 'active'
  )
);
```

#### 2.2 数据验证

```javascript
// 验证数据格式
function validateRow(row) {
  if (!row.phone || typeof row.phone !== 'string') {
    throw new Error('电话号码不能为空');
  }
  // 更多验证...
  return true;
}
```

### 3. XSS 防护

```javascript
// HTML 转义
function escapeHtml(str) {
  if (!str) return '';
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#039;');
}

// 使用转义函数
element.innerHTML = escapeHtml(userInput);
```

---

## 部署与维护

### 1. 部署步骤

#### 1.1 准备工作

1. 配置 Supabase 项目
2. 创建数据库表
3. 配置 RLS 策略
4. 设置环境变量

#### 1.2 部署到 GitHub Pages

```bash
# 1. 克隆仓库
git clone https://github.com/sevenoy/xhsNum.git
cd xhsNum

# 2. 配置 Supabase（在 js/app.js 中）
# 3. 更新版本号
node update-version.js

# 4. 提交并推送
git add .
git commit -m "更新版本号"
git push origin main

# 5. 在 GitHub 设置中启用 Pages
```

#### 1.3 部署到其他平台

**Vercel**:
```bash
npm i -g vercel
vercel
```

**Netlify**:
- 拖拽文件夹到 Netlify
- 或使用 Git 集成

### 2. 维护指南

#### 2.1 版本更新流程

1. 修改代码
2. 运行 `node update-version.js` 更新版本号
3. 更新 `update-log.json` 添加更新日志
4. 提交并推送到 GitHub
5. 用户打开应用时会自动检测更新

#### 2.2 数据库维护

**备份**:
```sql
-- 导出数据
pg_dump -h db.xxx.supabase.co -U postgres -d postgres > backup.sql
```

**清理旧数据**:
```sql
-- 删除过期快照（保留最近 100 条）
DELETE FROM xhsphone_snapshot
WHERE key != 'default'
AND updated_at < NOW() - INTERVAL '30 days'
AND id NOT IN (
  SELECT id FROM xhsphone_snapshot
  ORDER BY updated_at DESC
  LIMIT 100
);
```

#### 2.3 监控和日志

- 使用 Supabase Dashboard 监控数据库性能
- 使用浏览器控制台查看客户端日志
- 使用 Service Worker 日志调试缓存问题

---

## 总结

### 技术亮点

1. **离线优先架构**：数据首先存储在本地，支持离线使用
2. **实时同步**：基于 Supabase Realtime 的自动同步机制
3. **PWA 支持**：完整的 PWA 实现，支持添加到主屏幕
4. **版本管理**：完善的版本更新和检测机制
5. **权限系统**：基于角色的权限管理和资源级权限控制

### 适用场景

- 多用户协作的数据管理系统
- 需要离线使用的 Web 应用
- 需要实时同步的多设备应用
- 需要版本管理和快照功能的应用

### 扩展方向

1. **移动端原生应用**：使用 Capacitor 或 Cordova 打包为原生应用
2. **数据可视化**：添加数据统计和可视化功能
3. **导入导出增强**：支持更多格式（Excel、JSON 等）
4. **协作功能**：添加评论、@ 提醒等协作功能
5. **API 接口**：提供 RESTful API 供第三方集成

---

**文档版本**: V251217.67  
**最后更新**: 2025年12月17日  
**维护者**: sevenoy

