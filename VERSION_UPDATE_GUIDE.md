# 版本号更新指南

## 📋 概述

每次推送代码更改时，**必须**更新版本号，这样用户才能收到更新提示并确认更新。版本号格式为：`YYYYMMDD.N`（日期 + 序号）

## 🔄 更新版本号的步骤

### 1. 更新 `index.html` 中的版本号

找到以下代码（大约在第 256 行）：
```javascript
const APP_VERSION = '20250115.6';  // 修改这里的版本号
```

**规则**：
- 如果当天第一次更新，序号从 `.1` 开始
- 如果当天已有更新，序号递增（`.2`, `.3`, `.4`...）
- 如果跨天，日期改为当天日期，序号从 `.1` 开始

**示例**：
- 2025-01-15 第一次更新：`20250115.1`
- 2025-01-15 第二次更新：`20250115.2`
- 2025-01-16 第一次更新：`20250116.1`

### 2. 更新 `sw.js` 中的版本号

找到以下代码（第 4 行）：
```javascript
const VERSION = '20250115.6'; // ✅ 每次更新代码时，修改这个版本号
```

**重要**：`sw.js` 中的 `VERSION` 必须与 `index.html` 中的 `APP_VERSION` **完全相同**！

### 3. 更新 `update-log.json` 中的更新日志

在 `update-log.json` 文件的开头添加新版本的更新信息：

```json
{
  "20250115.6": {
    "version": "20250115.6",
    "date": "2025-01-15",
    "title": "更新标题（简要描述本次更新）",
    "content": [
      "✨ 新功能1",
      "🔧 修复问题1",
      "✅ 优化功能1"
    ]
  },
  "20250115.5": {
    ...
  }
}
```

**字段说明**：
- `version`: 版本号（与 `APP_VERSION` 相同）
- `date`: 更新日期（格式：`YYYY-MM-DD`）
- `title`: 更新标题（简要描述，会显示在提示框顶部）
- `content`: 更新内容列表（数组，每项一行，支持 emoji）

**内容格式建议**：
- `✨` 新功能
- `🔧` 修复问题
- `✅` 优化改进
- `🚀` 性能提升
- `📝` 文档更新

### 4. 提交和推送

```bash
git add index.html sw.js update-log.json
git commit -m "更新版本号到 20250115.6: [简要描述更新内容]"
git push
```

## ⚠️ 重要提醒

1. **必须同时更新三个文件**：
   - `index.html` 中的 `APP_VERSION`
   - `sw.js` 中的 `VERSION`
   - `update-log.json` 中的更新日志

2. **版本号必须一致**：
   - `index.html` 和 `sw.js` 中的版本号必须完全相同
   - 如果版本号不一致，会导致更新检测失败

3. **每次推送都要更新**：
   - 即使是很小的修改，也要更新版本号
   - 这样用户才能收到更新提示并确认更新
   - 方便对应问题和追踪更新历史

4. **更新日志要详细**：
   - 用户会看到更新内容，所以要写清楚本次更新做了什么
   - 使用 emoji 让内容更易读
   - 每项更新内容一行，简洁明了

## 📝 示例流程

假设今天是 2025-01-15，要修复一个 bug：

1. **修改代码**（修复 bug）

2. **更新版本号**：
   - `index.html`: `const APP_VERSION = '20250115.7';`
   - `sw.js`: `const VERSION = '20250115.7';`

3. **更新日志**：
   ```json
   {
     "20250115.7": {
       "version": "20250115.7",
       "date": "2025-01-15",
       "title": "修复重要问题",
       "content": [
         "🔧 修复 iPhone 16 Pro Max 无法自动更新的问题",
         "✅ 优化更新检测机制，确保所有设备都能检测到更新"
       ]
     },
     ...
   }
   ```

4. **提交推送**：
   ```bash
   git add index.html sw.js update-log.json
   git commit -m "更新版本号到 20250115.7: 修复更新检测问题"
   git push
   ```

5. **验证**：
   - 用户打开网站时会看到更新提示
   - 提示框显示版本号和更新内容
   - 用户点击"确定"后页面刷新，应用新版本

## 🎯 版本号的作用

1. **触发更新提示**：版本号变化会触发 Service Worker 更新检测
2. **显示更新内容**：从 `update-log.json` 读取对应版本的更新信息
3. **防止重复提示**：使用 localStorage 记录已显示的版本，避免重复提示
4. **问题追踪**：通过版本号可以快速定位问题发生的版本

## ❓ 常见问题

**Q: 如果忘记更新版本号会怎样？**
A: 用户不会收到更新提示，可能还在使用旧版本代码。

**Q: 版本号可以回退吗？**
A: 不建议回退，应该使用新的版本号。如果必须回退，确保三个文件都回退到相同版本。

**Q: 同一天可以更新多次吗？**
A: 可以，序号递增即可（`.1`, `.2`, `.3`...）。

**Q: 更新日志可以留空吗？**
A: 不建议，至少要写一个标题和一条更新内容，让用户知道更新了什么。
